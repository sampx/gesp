---
wave: 1
depends_on: []
files_modified:
  - packages/backend/package.json
  - packages/backend/tsconfig.json
  - packages/backend/src/index.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - package.json
  - turbo.json
autonomous: true
requirements_addressed: []
---

# Plan 01: Backend + Shared Package Initialization

**Objective:** Initialize monorepo structure with backend and shared packages, configuring Bun workspaces and Turborepo for Phase 1.

---

## Task 01-01: Create Root Monorepo Configuration

<read_first>
- .planning/PROJECT.md — 项目架构约束，monorepo 结构定义
- .planning/research/STACK.md — Turborepo + Bun workspaces 配置参考
</read_first>

<action>
Create root `package.json` with Bun workspaces configuration:
```json
{
  "name": "gesp-learning-platform",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "db:push": "bunx drizzle-kit push"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "bun@1.3.11"
}
```

Create `turbo.json` with pipeline configuration:
```json
{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
```
</action>

<acceptance_criteria>
- `package.json` contains `"workspaces": ["packages/*"]`
- `package.json` contains `"packageManager": "bun@1.3.11"`
- `turbo.json` contains `"pipeline"` with `"build"`, `"dev"`, `"typecheck"`, `"test"` keys
- `turbo.json` contains `"dependsOn": ["^build"]` for build pipeline
</acceptance_criteria>

---

## Task 01-02: Create Shared Package

<read_first>
- .planning/phases/01-基础设施与认证/01-CONTEXT.md — D-06: shared 仅包含类型定义与常量
</read_first>

<action>
Create `packages/shared/package.json`:
```json
{
  "name": "@gesp/shared",
  "version": "0.0.1",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
```

Create `packages/shared/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "declaration": true,
    "outDir": "./dist"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

Create `packages/shared/src/types/user.ts`:
```typescript
export interface User {
  id: string;
  username: string;
  password_hash: string;
  display_name: string;
  role: number; // 1=student, 10=admin, 100=root
  status: number; // 1=enabled, 2=disabled
  email?: string;
  github_id?: string;
  oidc_id?: string;
  wechat_id?: string;
  telegram_id?: string;
  created_at: Date;
  updated_at: Date;
}
```

Create `packages/shared/src/types/response.ts`:
```typescript
export interface ApiResponse<T = unknown> {
  success: boolean;
  message: string;
  data?: T;
}
```

Create `packages/shared/src/constants/role.ts`:
```typescript
export const ROLE = {
  STUDENT: 1,
  ADMIN: 10,
  ROOT: 100,
} as const;

export type RoleValue = typeof ROLE[keyof typeof ROLE];
```

Create `packages/shared/src/constants/status.ts`:
```typescript
export const USER_STATUS = {
  ENABLED: 1,
  DISABLED: 2,
} as const;

export type StatusValue = typeof USER_STATUS[keyof typeof USER_STATUS];
```

Create `packages/shared/src/index.ts`:
```typescript
export * from "./types/user";
export * from "./types/response";
export * from "./constants/role";
export * from "./constants/status";
```
</action>

<acceptance_criteria>
- `packages/shared/package.json` contains `"name": "@gesp/shared"`
- `packages/shared/src/types/user.ts` contains `interface User` with `id`, `username`, `password_hash`, `display_name`, `role`, `status`, `email`, `github_id`, `oidc_id`, `wechat_id`, `telegram_id`, `created_at`, `updated_at`
- `packages/shared/src/constants/role.ts` contains `ROLE.STUDENT = 1`, `ROLE.ADMIN = 10`, `ROLE.ROOT = 100`
- `packages/shared/src/index.ts` exports from `./types/user`, `./types/response`, `./constants/role`, `./constants/status`
</acceptance_criteria>

---

## Task 01-03: Create Backend Package

<read_first>
- .planning/research/STACK.md — Hono + Drizzle + Bun 配置参考
- .planning/phases/01-基础设施与认证/01-RESEARCH.md — Backend 目录结构参考
</read_first>

<action>
Create `packages/backend/package.json`:
```json
{
  "name": "@gesp/backend",
  "version": "0.0.1",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "hono-openapi": "^0.4.0",
    "drizzle-orm": "^0.39.0",
    "bcryptjs": "^3.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
```

Create `packages/backend/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "types": ["bun-types"],
    "paths": {
      "@gesp/shared": ["../shared/src"]
    }
  },
  "include": ["src/**/*", "drizzle/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

Create `packages/backend/bunfig.toml`:
```toml
[test]
preload = ["./src/__tests__/setup.ts"]
```

Create `packages/backend/src/index.ts` (initial skeleton):
```typescript
import { OpenAPIHono } from "hono-openapi";

const app = new OpenAPIHono();

app.get("/", (c) => c.json({ success: true, message: "GESP Backend API", data: { version: "0.0.1" } }));

// TODO: Mount auth routes
// app.route("/api/auth", authRoutes);
// app.route("/api/admin", adminRoutes);
// app.route("/api/student", studentRoutes);

// OpenAPI spec export
app.doc("/api/doc", {
  openapi: "3.0.0",
  info: {
    title: "GESP Learning Platform API",
    version: "1.0.0",
  },
});

const port = process.env.PORT || 3000;
console.log(`GESP Backend running on http://localhost:${port}`);

export default app;
```
</action>

<acceptance_criteria>
- `packages/backend/package.json` contains `"hono": "^4.0.0"` in dependencies
- `packages/backend/package.json` contains `"drizzle-orm": "^0.39.0"` in dependencies
- `packages/backend/package.json` contains `"bcryptjs": "^3.0.0"` in dependencies
- `packages/backend/src/index.ts` contains `import { OpenAPIHono } from "hono-openapi"`
- `packages/backend/src/index.ts` contains `app.doc("/api/doc", ...)`
</acceptance_criteria>

---

## Verification

<automated>
Run `bun install` from workspace root.
Run `bun run typecheck` — should pass with no errors.
Run `bun packages/backend/src/index.ts` — server should start and respond at http://localhost:3000.
</automated>

---

## must_haves

- Monorepo structure initialized (packages/backend + packages/shared)
- Bun workspaces configured
- Turborepo pipeline defined
- Backend package with Hono skeleton
- Shared package with types and constants