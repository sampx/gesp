---
phase: 03-测评定级智能体-测评界面
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/seed/assessment-questions.seed.ts
  - packages/backend/src/__tests__/assessment-question-bank.test.ts
autonomous: true
gap_closure: true
requirements: [ASSESS-02]
must_haves:
  truths:
    - "题库覆盖 GESP C++ 1-8 级，所有级别都能被自适应算法选到题"
    - "默认 5 题/轮的测评不会在任一级别因题量不足而提前耗尽题目"
    - "每级题型分布与 D-16 一致：3 道客观题 + 2 道编程题"
  artifacts:
    - path: packages/backend/src/seed/assessment-questions.seed.ts
      provides: 1-8 级测评题 seed 数据
      contains: "level: 8"
    - path: packages/backend/src/__tests__/assessment-question-bank.test.ts
      provides: 题库覆盖与题型配比验证
      contains: "5 questions per level"
  key_links:
    - from: packages/backend/src/seed/assessment-questions.seed.ts
      to: packages/backend/src/services/assessment.ts
      via: level + question_type + status='active'
      pattern: "level"
---

<objective>
补齐测评题库覆盖，让 1-8 级都具备可测题目，并让默认 5 题/轮的算法不再因题量不足失真。

Purpose: 关闭“高等级无题可测 + 单级题目不足”根因，恢复 ASSESS-02 的真实可用性。
Output: 可验证的 1-8 级题库 seed 与覆盖测试。
</objective>

<execution_context>
@.planning/phases/03-测评定级智能体-测评界面/03-UAT.md
@.planning/phases/03-测评定级智能体-测评界面/03-04-SUMMARY.md
@.planning/phases/03-测评定级智能体-测评界面/03-CONTEXT.md
</execution_context>

<context>
@packages/backend/src/seed/assessment-questions.seed.ts
@packages/backend/src/services/assessment.ts
@packages/backend/src/db/schema/assessment.ts

<interfaces>
From packages/backend/src/services/assessment.ts:
```ts
const DEFAULT_QUESTION_LIMIT = 5;
const DEFAULT_TYPE_RATIO = { objective: 3, coding: 2 };
```

From packages/backend/src/seed/assessment-questions.seed.ts:
```ts
interface SeedQuestion {
  course_id: string;
  level: number;
  knowledge_point: string;
  question_type: 'objective' | 'coding';
  difficulty: number;
  content: string;
  answer: string;
  explanation: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic coverage test for assessment question bank</name>
  <files>packages/backend/src/__tests__/assessment-question-bank.test.ts, packages/backend/src/seed/assessment-questions.seed.ts</files>
  <behavior>
    - Test 1: Question bank contains levels 1 through 8 with no gaps.
    - Test 2: Each level has exactly 5 active questions.
    - Test 3: Each level has 3 objective questions and 2 coding questions.
    - Test 4: Every seeded question has non-empty knowledge_point, content, answer, and explanation.
  </behavior>
  <action>
Create `packages/backend/src/__tests__/assessment-question-bank.test.ts` that imports an exported seed constant/helper from `packages/backend/src/seed/assessment-questions.seed.ts` and validates the bank shape directly, without touching SQLite or LanceDB.

In `assessment-questions.seed.ts`, expose the seed data through a named export (`ASSESSMENT_QUESTIONS` or equivalent) so the test can assert bank coverage deterministically. Keep the seed runtime behavior unchanged.

This task exists to lock the round-size invariant before expanding content: the algorithm uses 5 questions per round, so the bank must prove every level can satisfy one full round.
  </action>
  <verify>
    <automated>cd packages/backend && bun test src/__tests__/assessment-question-bank.test.ts</automated>
  </verify>
  <done>Coverage test fails before data expansion and defines the exact 1-8 / 5-question / 3:2 constraints the seed must satisfy.</done>
</task>

<task type="auto">
  <name>Task 2: Expand seed data to full 1-8 coverage with round-safe inventory</name>
  <files>packages/backend/src/seed/assessment-questions.seed.ts</files>
  <action>
Update `packages/backend/src/seed/assessment-questions.seed.ts` so the final bank contains 40 active questions total: 5 questions per level across levels 1-8, with 3 `objective` and 2 `coding` questions per level per D-16.

Specific implementation requirements:
- Keep existing levels 1-4 but add the missing fifth question per level if any level still has only 4 entries.
- Add complete level sets for 5, 6, 7, and 8, each covering GESP-appropriate C++ topics for that level rather than generic placeholders.
- Preserve current seed shape, idempotent insertion logic, and LanceDB embedding text pipeline.
- Mark all added questions `status: "active"` and `created_by: "manual"` to keep candidate selection working immediately.
- Keep `course_id: "cpp"` for all records in this phase.

Do not add placeholder wording, TODO content, or “future enhancement” entries inside the bank. The seed must be production-usable for real assessments.
  </action>
  <verify>
    <automated>cd packages/backend && bun test src/__tests__/assessment-question-bank.test.ts && bun run typecheck</automated>
  </verify>
  <done>Seed data provides 5 active questions for every level 1-8, matching the configured 3-objective/2-coding round shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed source → SQLite/LanceDB | Manual seed content becomes live assessment material |
| Question bank → adaptive selector | Invalid level/type metadata would skew routing and level decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | assessment-questions.seed.ts | mitigate | Add deterministic bank-shape test for 1-8 coverage and 3:2 type ratio before relying on runtime seed. |
| T-03-06-02 | Information Disclosure | assessment question content | accept | Seed file is repository content for internal developers; no new exposure path is introduced by this plan. |
| T-03-06-03 | Denial of Service | candidate selection at L5-L8 | mitigate | Ensure every level has enough active questions for one full round so `getCandidates()` never returns empty solely due to missing seed data. |
</threat_model>

<verification>
1. `cd packages/backend && bun test src/__tests__/assessment-question-bank.test.ts`
2. `cd packages/backend && bun run typecheck`
</verification>

<success_criteria>
- Levels 1-8 all have active assessment questions.
- Every level can satisfy one default assessment round without reusing a question.
- Seed coverage is enforced by automated test instead of manual counting.
</success_criteria>

<output>
After completion, create `.planning/phases/03-测评定级智能体-测评界面/03-06-SUMMARY.md`
</output>
