---
phase: 03-测评定级智能体-测评界面
plan: "10"
type: execute
wave: 3
depends_on:
  - "03-07"
  - "03-08"
files_modified:
  - apps/web/src/app/student/layout.tsx
  - apps/web/src/app/student/assessment/[token]/page.tsx
  - apps/web/src/components/student-navbar.tsx
  - apps/web/src/components/assessment/progress-bar.tsx
  - apps/web/src/components/assessment/chat-panel.tsx
  - apps/web/src/components/assessment/chat-panel-context.tsx
autonomous: true
gap_closure: true
requirements: [UI-ASSESS-01, ASSESS-04, ASSESS-05]
must_haves:
  truths:
    - "答题页进度栏正确显示当前题号、正确数/已答题数、总题数、剩余题数、总时长、剩余时长"
    - "AI agent logo 出现在 header 中用户头像左侧，而不是左下角悬浮按钮"
    - "反馈阶段只有在下一题真正准备好后才允许进入下一题，点击时不会再卡住等 skeleton"
    - "智能体终止测评后，答题页会进入 DONE/报告跳转，而不是继续显示新题目"
  artifacts:
    - path: apps/web/src/components/assessment/progress-bar.tsx
      provides: 扩展进度信息与正确题号显示
      contains: "remainingTime"
    - path: apps/web/src/components/student-navbar.tsx
      provides: assessment header agent logo slot
      contains: "Bot"
    - path: apps/web/src/components/assessment/chat-panel-context.tsx
      provides: header-toggle chat state sharing
      contains: "createContext"
    - path: apps/web/src/app/student/assessment/[token]/page.tsx
      provides: ready-aware next-question prefetch and done redirect
      contains: "questionReady"
  key_links:
    - from: apps/web/src/components/student-navbar.tsx
      to: apps/web/src/components/assessment/chat-panel-context.tsx
      via: header toggle and unread state
      pattern: "useAssessmentChat"
    - from: apps/web/src/components/assessment/chat-panel.tsx
      to: apps/web/src/app/student/assessment/[token]/page.tsx
      via: question_ready / assessment_done callbacks
      pattern: "question_ready|assessment_done"
---

<objective>
修复答题页核心体验：让进度信息真实、chat 入口回到 header、下一题按钮基于 ready 信号点亮，并在测评结束时稳定跳转报告页。

Purpose: 关闭用户在答题过程中最直观的 UI/交互痛点。
Output: 准确进度栏、header chat 入口、ready-aware 下一题流程、可靠 done 跳转。
</objective>

<execution_context>
@.planning/phases/03-测评定级智能体-测评界面/03-UAT.md
@apps/web/src/app/student/assessment/[token]/page.tsx
@apps/web/src/components/assessment/chat-panel.tsx
@apps/web/src/components/student-navbar.tsx
</execution_context>

<context>
@apps/web/src/app/student/layout.tsx
@apps/web/src/app/student/assessment/[token]/page.tsx
@apps/web/src/components/student-navbar.tsx
@apps/web/src/components/assessment/chat-panel.tsx
@apps/web/src/components/assessment/progress-bar.tsx

<interfaces>
From Plan 08 SSE contract:
```ts
type NormalizedEvent =
  | { type: 'question_ready' }
  | { type: 'assessment_done'; final_level?: number }
  | { type: 'message_delta'; ... }
```

From Plan 07 progress contract:
```ts
type ProgressData = {
  total_answered: number;
  total_correct: number;
  config_question_limit: number;
  config_time_limit_min: number;
  remaining_questions: number;
  remaining_time_sec: number;
  started_at: string;
  done: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct progress-bar data model and display</name>
  <files>apps/web/src/app/student/assessment/[token]/page.tsx, apps/web/src/components/assessment/progress-bar.tsx</files>
  <action>
Refactor the answer-page progress model so it stops fabricating counts client-side.

Specific implementation:
- In `apps/web/src/app/student/assessment/[token]/page.tsx`, stop incrementing `total_answered` / `total_correct` optimistically during FEEDBACK. Instead, refresh local progress from backend payloads and maintain a dedicated `currentQuestionNumber` / `activeQuestionOrder` state for the currently displayed question.
- In `apps/web/src/components/assessment/progress-bar.tsx`, expand props and rendering to show:
  - current level
  - current question number
  - correct / answered
  - total question limit
  - remaining question count
  - total time limit
  - remaining time
- Add a client-side ticking countdown derived from backend `started_at` + `config_time_limit_min`, but keep backend values as the source of truth on refresh.
- Ensure FEEDBACK state keeps showing the just-answered question number instead of `totalAnswered + 1`.

This task fixes the off-by-one bug and the missing time/remaining metadata from Gap 1.
  </action>
  <verify>
    <automated>cd apps/web && bun run typecheck</automated>
  </verify>
  <done>Progress bar reflects real assessment state in answering, judging, and feedback phases without off-by-one errors.</done>
</task>

<task type="auto">
  <name>Task 2: Move chat toggle into student header beside the user avatar</name>
  <files>apps/web/src/app/student/layout.tsx, apps/web/src/components/student-navbar.tsx, apps/web/src/components/assessment/chat-panel.tsx, apps/web/src/components/assessment/chat-panel-context.tsx</files>
  <action>
Implement a shared chat-toggle context so the assessment chat entry lives in the header, matching the latest UAT decision even though the earlier Phase 3 context used a floating toggle.

Implementation details:
- Create `apps/web/src/components/assessment/chat-panel-context.tsx` exporting a provider + hook that stores `open`, `setOpen`, `unread`, and `clearUnread`.
- Wrap the student layout content in this provider from `apps/web/src/app/student/layout.tsx` so both `StudentNavbar` and `ChatPanel` share the same state.
- In `apps/web/src/components/student-navbar.tsx`, use `usePathname()` to render a Bot button only on `/student/assessment` routes, positioned immediately left of the avatar trigger. The unread badge belongs on this header button.
- In `apps/web/src/components/assessment/chat-panel.tsx`, remove the fixed bottom-left toggle button entirely and consume the shared context for open/unread state.

Do not keep two toggles. The header icon is now the single source of truth for opening/closing chat on assessment pages.
  </action>
  <verify>
    <automated>cd apps/web && bun run typecheck && grep -n "chat-panel-context\|usePathname\|Bot" src/components/student-navbar.tsx src/components/assessment/chat-panel.tsx src/components/assessment/chat-panel-context.tsx</automated>
  </verify>
  <done>Assessment pages use a single header chat toggle left of the user avatar, with unread count carried in the header instead of a floating corner button.</done>
</task>

<task type="auto">
  <name>Task 3: Gate the Next button on question readiness and redirect cleanly on done</name>
  <files>apps/web/src/app/student/assessment/[token]/page.tsx, apps/web/src/components/assessment/chat-panel.tsx</files>
  <action>
Wire the answer page to the new backend readiness events so FEEDBACK becomes “read while prefetching”, not “click then block”.

Implementation requirements:
- Extend `apps/web/src/components/assessment/chat-panel.tsx` to recognize `question_ready` and `assessment_done` SSE events in addition to normal chat payloads, and expose them upward through callback props.
- In `apps/web/src/app/student/assessment/[token]/page.tsx`, when FEEDBACK starts, wait for `question_ready`; once received, prefetch `/api/assessment/next-question` in the background and store the result.
- Render the next button disabled (or as a loading CTA) until the prefetched question exists. Clicking it should swap to the prefetched question immediately instead of entering a fresh blocking skeleton state.
- If `assessment_done` arrives, or if `/next-question` responds with `done: true`, transition to DONE and `router.replace(`/student/assessment/${token}/report`)` after showing the completion state briefly.
- On page refresh, if progress already says `done`, skip question loading and route to the report path.

This task closes both the “下一题按钮不能提前亮起” and “终止评价后仍出现新题目” gaps.
  </action>
  <verify>
    <automated>cd apps/web && bun run typecheck</automated>
  </verify>
  <done>反馈阶段可以后台预取下一题，Next 只在 ready 后可点，完成信号会稳定跳转报告页且不再显示幽灵题目。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE system events → UI state | Backend readiness/done events directly drive navigation and button enablement |
| Header toggle context → chat panel | Shared client state coordinates navigation shell and assessment chat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10-01 | Tampering | question_ready handling | mitigate | Only enable Next after a successful prefetched `/next-question` response tied to the authenticated token, not on optimistic UI timers. |
| T-03-10-02 | Information Disclosure | header chat badge | accept | Badge exposes unread count only; no question content or scoring data leaks into the navbar. |
| T-03-10-03 | Denial of Service | repeated SSE reconnects | accept | Keep EventSource auto-reconnect behavior and idempotent event handling; ready/done handlers must tolerate duplicate events safely. |
</threat_model>

<verification>
1. `cd apps/web && bun run typecheck`
2. Verify `question_ready` / `assessment_done` handling exists in `chat-panel.tsx`
3. Verify `chat-panel-context.tsx` is imported by both `student/layout.tsx` and `student-navbar.tsx`
</verification>

<success_criteria>
- Progress UI is accurate throughout the answer lifecycle.
- Chat toggle is in the header left of the avatar on assessment pages.
- Next button only becomes actionable after backend readiness.
- Final evaluation transitions to report instead of rendering another question.
</success_criteria>

<output>
After completion, create `.planning/phases/03-测评定级智能体-测评界面/03-10-SUMMARY.md`
</output>
