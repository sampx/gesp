---
phase: 03-测评定级智能体-测评界面
plan: "12"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/student/assessment/page.tsx
  - apps/web/src/components/assessment/objective-question.tsx
  - apps/web/src/components/student-navbar.tsx
autonomous: true
gap_closure: true
requirements: [UI-ASSESS-01]
must_haves:
  truths:
    - "起测页课程下拉框初始显示 human-readable label，而不是 raw value cpp"
    - "切换到新客观题时，不会残留上一题的默认选中状态"
    - "测评子页面提供明确返回入口，不必依赖浏览器后退按钮"
  artifacts:
    - path: apps/web/src/app/student/assessment/page.tsx
      provides: 课程下拉显示文案映射
      contains: "C++ 编程"
    - path: apps/web/src/components/assessment/objective-question.tsx
      provides: 新题渲染时清空旧选项状态
      contains: "useEffect"
    - path: apps/web/src/components/student-navbar.tsx
      provides: assessment-route back button
      contains: "ArrowLeft"
  key_links:
    - from: apps/web/src/app/student/assessment/page.tsx
      to: apps/web/src/components/ui/select.tsx
      via: Select trigger display label mapping
      pattern: "SelectValue"
    - from: apps/web/src/components/student-navbar.tsx
      to: student assessment routes
      via: pathname-based back target
      pattern: "/student/assessment"
---

<objective>
修复 Phase 03 剩余的轻量 UI 缺陷：课程文案、客观题默认选中残留、测评流程缺少返回按钮。

Purpose: 关闭 Test 2 / Test 4 / Test 10，让测评页面基本交互更稳定可用。
Output: 一致的课程显示、无残留选择状态、assessment 子页面显式返回入口。
</objective>

<execution_context>
@.planning/phases/03-测评定级智能体-测评界面/03-UAT.md
@.planning/debug/course-dropdown-mismatch.md
@.planning/debug/objective-default-selection.md
@.planning/debug/missing-back-button.md
</execution_context>

<context>
@apps/web/src/app/student/assessment/page.tsx
@apps/web/src/components/assessment/objective-question.tsx
@apps/web/src/components/student-navbar.tsx
@.planning/phases/02.1-用户基础流程补齐/02.1-02-SUMMARY.md

<interfaces>
From apps/web/src/components/assessment/objective-question.tsx:
```ts
interface ObjectiveQuestionProps {
  content: string;
  disabled: boolean;
  onAnswer: (answer: string) => void;
}
```

From apps/web/src/components/student-navbar.tsx:
```ts
const pathname = usePathname();
const isAssessment = pathname.startsWith("/student/assessment");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix course label rendering and reset objective selection on question change</name>
  <read_first>
    - apps/web/src/app/student/assessment/page.tsx
    - apps/web/src/components/assessment/objective-question.tsx
    - .planning/debug/course-dropdown-mismatch.md
    - .planning/debug/objective-default-selection.md
  </read_first>
  <files>apps/web/src/app/student/assessment/page.tsx, apps/web/src/components/assessment/objective-question.tsx</files>
  <action>In `apps/web/src/app/student/assessment/page.tsx`, replace the raw `SelectValue` display for `courseId` with an explicit course option map so the closed trigger renders `C++ 编程` while still submitting the underlying `cpp` value. In `apps/web/src/components/assessment/objective-question.tsx`, clear internal `selected` state whenever the question content changes and immediately call `onAnswer("")` on reset so the parent and child stay aligned. Do not rely on stale component instance reuse. The reset must happen automatically for every new objective question without requiring a full page reload.</action>
  <acceptance_criteria>
    - `grep -n "C\+\+ 编程" apps/web/src/app/student/assessment/page.tsx` shows explicit label mapping for the closed select trigger
    - `grep -n "useEffect\|setSelected\(\"\"\)" apps/web/src/components/assessment/objective-question.tsx` shows question-change reset logic
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && bun run typecheck</automated>
  </verify>
  <done>起测页不再显示 raw value `cpp`；切题后客观题默认选中状态会被清空。</done>
</task>

<task type="auto">
  <name>Task 2: Add a deterministic back button for assessment subpages</name>
  <read_first>
    - apps/web/src/components/student-navbar.tsx
    - .planning/debug/missing-back-button.md
    - .planning/phases/02.1-用户基础流程补齐/02.1-02-SUMMARY.md
  </read_first>
  <files>apps/web/src/components/student-navbar.tsx</files>
  <action>Extend `apps/web/src/components/student-navbar.tsx` with a single assessment-only back button shown on subroutes under `/student/assessment` except the start page itself. Use explicit route targets instead of `router.back()`: for any assessment detail or report route, send the user to `/student/assessment`. Place the button at the left side of the navbar before the product title, use a compact icon+label affordance, and keep the existing Bot toggle + avatar menu behavior unchanged.</action>
  <acceptance_criteria>
    - `grep -n "ArrowLeft\|/student/assessment" apps/web/src/components/student-navbar.tsx` shows explicit back-button implementation
    - `cd apps/web && bun run typecheck` passes after navbar route-guard updates
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && bun run typecheck && grep -n "ArrowLeft\|/student/assessment" src/components/student-navbar.tsx</automated>
  </verify>
  <done>测评答题页和报告页都有明确返回入口，用户不再只能依赖浏览器后退按钮。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client pathname → navbar navigation target | 客户端路由判断直接决定返回目标 |
| client component state → answer submission | 题目切换时需要同步清空本地选项状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12-01 | Tampering | objective local state | mitigate | 当题目 content 变化时强制清空 `selected` 与父级 answer，避免旧答案污染新题提交。 |
| T-03-12-02 | Spoofing | course display label | accept | 仅前端展示 label 映射，不改变提交值 `cpp`，不会影响后端鉴权或数据归属。 |
| T-03-12-03 | Denial of Service | navbar back navigation | accept | 使用固定目标 `/student/assessment`，避免 `router.back()` 因历史栈缺失导致无效导航。 |
</threat_model>

<verification>
1. `cd apps/web && bun run typecheck`
2. `grep -n "C\+\+ 编程" apps/web/src/app/student/assessment/page.tsx`
3. `grep -n "ArrowLeft\|setSelected\(\"\"\)" apps/web/src/components/student-navbar.tsx apps/web/src/components/assessment/objective-question.tsx`
</verification>

<success_criteria>
- Select trigger shows the same course label as its dropdown item.
- Objective questions open with no stale default selection.
- Assessment subpages expose a clear back path to `/student/assessment`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-测评定级智能体-测评界面/03-12-SUMMARY.md`
</output>
