---
phase: 01.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/middleware/session.ts
  - packages/backend/src/services/auth.service.ts
  - packages/backend/src/db/seed/admin.seed.ts
  - packages/backend/src/__tests__/session-id.test.ts
  - packages/backend/src/__tests__/auth-register.test.ts
  - packages/backend/src/__tests__/seed-password.test.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Session ID 使用 crypto.getRandomValues(32 bytes) + base64url 生成，非 UUID"
    - "注册失败时错误消息不泄露用户存在性"
    - "生产环境未设置 ADMIN_PASSWORD 时应用拒绝启动"
    - "所有安全修复通过单元测试验证"
  artifacts:
    - path: "packages/backend/src/middleware/session.ts"
      provides: "Session ID 生成函数使用 crypto.getRandomValues"
      contains: "generateSessionId"
    - path: "packages/backend/src/services/auth.service.ts"
      provides: "注册错误消息模糊化"
      contains: "Registration failed"
    - path: "packages/backend/src/db/seed/admin.seed.ts"
      provides: "生产环境 ADMIN_PASSWORD 强制检查"
      contains: "ADMIN_PASSWORD must be set"
  key_links:
    - from: "packages/backend/src/middleware/session.ts"
      to: "packages/backend/src/__tests__/session-id.test.ts"
      via: "export function generateSessionId"
      pattern: "generateSessionId"
    - from: "packages/backend/src/services/auth.service.ts"
      to: "packages/backend/src/__tests__/auth-register.test.ts"
      via: "registerUser return error message"
      pattern: "Username already exists"
---

<objective>
修复 Phase 1 评审中发现的 3 项安全问题（D-R4、D-R5、D-R7），并为每项修复编写单元测试验证。

Purpose: 消除 session 生成、用户枚举、默认密码三类安全风险
Output: 3 个修复文件 + 3 个测试文件
</objective>

<execution_context>
@/Users/sam/coding/wopal/wopal-workspace/.agents/wsf/workflows/execute-plan.md
@/Users/sam/coding/wopal/wopal-workspace/.agents/wsf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/backend/AGENTS.md
@.planning/phases/01.1-phase-1/01.1-RESEARCH.md
@.planning/phases/01.1-phase-1/01.1-VALIDATION.md
</context>

<interfaces>
<!-- Key interfaces extracted from files executor will modify -->

From packages/backend/src/middleware/session.ts:
```typescript
export async function createSession(c: Context, userId: string, role: number): Promise<string>
// Line 21: const sessionId = crypto.randomUUID();  ← THIS LINE MUST CHANGE
export async function validateSession(c: Context, next: Next): Promise<Response | void>
export async function destroySession(c: Context): Promise<void>
// SESSION_TTL: student=3600, admin/root=86400
```

From packages/backend/src/services/auth.service.ts:
```typescript
export async function registerUser(username: string, password: string, displayName: string)
  : Promise<{ success: boolean; user?: typeof users.$inferSelect; error?: string }>
// Line 18: return { success: false, error: "Username already exists" };  ← THIS LINE MUST CHANGE
export async function loginUser(username: string, password: string)
  : Promise<{ success: boolean; user?: typeof users.$inferSelect; error?: string }>
export async function getUserById(userId: string): Promise<typeof users.$inferSelect | null>
```

From packages/backend/src/db/seed/admin.seed.ts:
```typescript
export async function seedAdmin(): Promise<void>
// Line 20: const password = process.env.ADMIN_PASSWORD || "admin123";  ← THIS MUST CHANGE
export async function runSeeds(): Promise<void>  // calls seedAdmin()
```

From packages/backend/src/__tests__/password.test.ts (test pattern reference):
```typescript
import { describe, it, expect } from "vitest";
// Tests use direct function calls, no HTTP layer
```

From packages/backend/src/utils/response.ts (used by routes):
```typescript
export function success(c, data?, message?)   // 200
export function error(c, message, status?)    // 400
export function unauthorized(c, message?)     // 401
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session ID 改用 crypto.getRandomValues + base64url (D-R4)</name>
  <files>packages/backend/src/middleware/session.ts, packages/backend/src/__tests__/session-id.test.ts</files>
  <read_first>
    1. packages/backend/src/middleware/session.ts — 当前实现使用 crypto.randomUUID()
    2. packages/backend/AGENTS.md — 代码规范（禁止 console.log，使用 logger）
  </read_first>
  <behavior>
    - Test 1: generateSessionId() 返回长度为 43 的 base64url 字符串（32 bytes → 43 chars）
    - Test 2: generateSessionId() 仅包含 [A-Za-z0-9_-] 字符
    - Test 3: 连续两次调用 generateSessionId() 返回不同值（随机性）
    - Test 4: createSession() 创建的 session ID 符合 43 字符 base64url 格式
  </behavior>
  <action>
    在 `packages/backend/src/middleware/session.ts` 中：

    1. 在文件顶部（imports 之后、SESSION_TTL 之前）添加导出函数：
    ```typescript
    /**
     * Generate a cryptographically secure session ID.
     * Uses 32 random bytes encoded as base64url (43 chars).
     * Per D-R4: replaces crypto.randomUUID() for higher entropy.
     */
    export function generateSessionId(): string {
      const bytes = new Uint8Array(32);
      crypto.getRandomValues(bytes);
      return Buffer.from(bytes).toString("base64url");
    }
    ```

    2. 修改 `createSession` 函数第 21 行，将：
    ```typescript
    const sessionId = crypto.randomUUID();
    ```
    替换为：
    ```typescript
    const sessionId = generateSessionId();
    ```

    不修改任何其他代码。保持 validateSession 和 destroySession 不变。
  </action>
  <verify>
    <automated>cd /Users/sam/coding/wopal/wopal-workspace/projects/gesp && bun run test --run src/__tests__/session-id.test.ts</automated>
  </verify>
  <done>
    - generateSessionId() 已导出且被 createSession() 调用
    - 4 个测试全部通过
    - session.ts 中不存在 crypto.randomUUID() 的调用
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 注册错误消息模糊化 (D-R5)</name>
  <files>packages/backend/src/services/auth.service.ts, packages/backend/src/__tests__/auth-register.test.ts</files>
  <read_first>
    1. packages/backend/src/services/auth.service.ts — 第 18 行当前返回 "Username already exists"
    2. packages/backend/src/__tests__/password.test.ts — 现有测试模式参考
  </read_first>
  <behavior>
    - Test 1: registerUser() 在用户名已存在时返回的错误消息包含 "failed" 但不包含 "already exists"
    - Test 2: registerUser() 在用户名已存在时返回的错误消息不包含 "Username"
    - Test 3: registerUser() 在密码过短时返回的错误消息保持不变（包含 "Password must be at least 6 characters"）— 不影响非安全类错误消息
  </behavior>
  <action>
    在 `packages/backend/src/services/auth.service.ts` 中：

    修改第 17-19 行，将：
    ```typescript
    if (existing) {
      return { success: false, error: "Username already exists" };
    }
    ```
    替换为：
    ```typescript
    if (existing) {
      return { success: false, error: "Registration failed. Please try different credentials." };
    }
    ```

    不修改任何其他代码。密码长度校验消息保持原样（这不是安全敏感信息泄露）。
  </action>
  <verify>
    <automated>cd /Users/sam/coding/wopal/wopal-workspace/projects/gesp && bun run test --run src/__tests__/auth-register.test.ts</automated>
  </verify>
  <done>
    - auth.service.ts 中不存在 "Username already exists" 字符串
    - 新错误消息为 "Registration failed. Please try different credentials."
    - 3 个测试全部通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 生产环境强制 ADMIN_PASSWORD (D-R7)</name>
  <files>packages/backend/src/db/seed/admin.seed.ts, packages/backend/src/__tests__/seed-password.test.ts</files>
  <read_first>
    1. packages/backend/src/db/seed/admin.seed.ts — 当前无生产环境检查
    2. packages/backend/src/__tests__/seed.test.ts — 现有 seed 测试（需要更新兼容）
    3. packages/backend/AGENTS.md — 禁止 console.log，使用 logger（但 seed 文件是启动流程，使用 console.warn 可接受，因为 logger 可能尚未初始化）
  </read_first>
  <behavior>
    - Test 1: NODE_ENV=production 且无 ADMIN_PASSWORD 时，seedAdmin() 抛出 Error，消息包含 "ADMIN_PASSWORD must be set"
    - Test 2: NODE_ENV=production 且有 ADMIN_PASSWORD 时，seedAdmin() 正常执行不抛错
    - Test 3: NODE_ENV=development 且无 ADMIN_PASSWORD 时，seedAdmin() 正常执行不抛错（使用默认值）
    - Test 4: seedAdmin() 在 admin 已存在时跳过创建（无论环境）
  </behavior>
  <action>
    在 `packages/backend/src/db/seed/admin.seed.ts` 中完整替换 `seedAdmin` 函数体：

    ```typescript
    export async function seedAdmin(): Promise<void> {
      // Check if root admin already exists
      const existingRoot = await db.query.users.findFirst({
        where: eq(users.role, ROLE.ROOT),
      });

      if (existingRoot) {
        console.log("Admin already exists, skipping seed");
        return;
      }

      // Production: require ADMIN_PASSWORD
      const password = process.env.ADMIN_PASSWORD;
      if (process.env.NODE_ENV === "production" && !password) {
        throw new Error("ADMIN_PASSWORD must be set in production environment");
      }

      const username = process.env.ADMIN_USERNAME || "admin";
      const displayName = process.env.ADMIN_DISPLAY_NAME || "System Admin";

      // Development: warn when using default password
      if (!password) {
        console.warn("WARNING: Using default password 'admin123'. CHANGE IMMEDIATELY!");
        console.warn("WARNING: Set ADMIN_PASSWORD environment variable for security.");
      }

      // Hash password
      const passwordHash = await hashPassword(password || "admin123");

      // Insert root admin
      await db.insert(users).values({
        id: crypto.randomUUID(),
        username,
        password_hash: passwordHash,
        display_name: displayName,
        role: ROLE.ROOT,
        status: USER_STATUS.ENABLED,
      });

      console.log(`Root admin seeded: ${username}`);
    }
    ```

    关键变更：
    - 第 14-16 行新增：`if (process.env.NODE_ENV === "production" && !password)` → 抛出 Error
    - 第 25-28 行新增：开发环境默认密码时输出 console.warn 警告
    - 第 30 行改为：`await hashPassword(password || "admin123")`
    - 删除第 36-37 行旧的两条 console.log/console.warn（WARNING 行）
  </action>
  <verify>
    <automated>cd /Users/sam/coding/wopal/wopal-workspace/projects/gesp && bun run test --run src/__tests__/seed-password.test.ts</automated>
  </verify>
  <done>
    - admin.seed.ts 包含 "ADMIN_PASSWORD must be set" 错误消息
    - admin.seed.ts 包含 "production" 环境检查
    - admin.seed.ts 包含 "CHANGE IMMEDIATELY" 开发环境警告
    - 4 个测试全部通过
    - 现有 seed.test.ts 仍然通过（`bun run test --run`）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/auth/register | Untrusted: username, password, display_name from client |
| client → /api/auth/login | Untrusted: username, password from client |
| env vars → seedAdmin | Semi-trusted: ADMIN_PASSWORD from deployment config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Information Disclosure | auth.service.ts registerUser | mitigate | D-R5: Replace "Username already exists" with generic message to prevent user enumeration |
| T-01.1-02 | Spoofing | session.ts createSession | mitigate | D-R4: Replace crypto.randomUUID() (122-bit entropy) with crypto.getRandomValues(32 bytes) (256-bit entropy) + base64url encoding |
| T-01.1-03 | Security Misconfiguration | admin.seed.ts seedAdmin | mitigate | D-R7: Block startup in production without ADMIN_PASSWORD env var |
| T-01.1-04 | Information Disclosure | auth.service.ts loginUser | accept | D-R6: Already implemented — "Invalid username or password" is generic. No fix needed. |
| T-01.1-05 | Denial of Service | /debug endpoint | accept | D-R8: Debug-only route, development use. Production disabled by default. |
</threat_model>

<verification>
1. `bun run test --run` — 全部测试通过（含 3 个新测试文件 + 现有测试）
2. `grep -r "crypto.randomUUID()" packages/backend/src/middleware/session.ts` — 无匹配（已替换）
3. `grep -r "Username already exists" packages/backend/src/services/auth.service.ts` — 无匹配（已替换）
4. `grep -r "ADMIN_PASSWORD must be set" packages/backend/src/db/seed/admin.seed.ts` — 有匹配
5. `bun run typecheck` — 类型检查通过
</verification>

<success_criteria>
- Session ID 为 43 字符 base64url 格式（32 字节随机值）
- 注册失败不泄露用户存在性
- 生产环境无 ADMIN_PASSWORD 时 seedAdmin 抛出 Error
- 所有现有测试仍然通过
- 所有新测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-phase-1/01.1-01-SUMMARY.md`
</output>
