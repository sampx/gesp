---
phase: 02.1-用户基础流程补齐
plan: 01
subsystem: auth, api
tags: [hono, drizzle, zod, bcryptjs, sqlite, admin-crud]

requires:
  - phase: 01-基础设施与认证
    provides: auth routes, session middleware, role constants, user schema
provides:
  - Seed migration: admin → root username, ROOT_* env vars
  - registerUserWithRole service with STUDENT/ADMIN role support
  - changePassword service and POST /api/auth/change-password endpoint
  - Admin user management CRUD API (list/create/toggle-status/reset-password)
affects: [02.1-02, 02.1-03]

tech-stack:
  added: []
  patterns: [admin-crud-route, paginated-user-list, role-based-registration]

key-files:
  created:
    - packages/backend/src/routes/admin-users.ts
  modified:
    - packages/backend/src/db/seed/admin.seed.ts
    - packages/backend/src/services/auth.service.ts
    - packages/backend/src/routes/auth.ts
    - packages/backend/src/index.ts
    - packages/backend/src/__tests__/auth-register.test.ts
    - packages/backend/src/__tests__/seed-password.test.ts

key-decisions:
  - "registerUserWithRole rejects ROOT(100), only allows STUDENT(1) and ADMIN(10)"
  - "display_name defaults to username for all registration flows"
  - "registerUser backward compat: internally calls registerUserWithRole with ROLE.STUDENT"
  - "Admin CRUD endpoints all use AdminAuth (role >= 10), per D-17 Phase 2.1 no permission restriction"
  - "User list excludes password_hash from query results"

patterns-established:
  - "Admin CRUD route pattern: AdminAuth + zValidator + describeRoute + service function"
  - "Paginated list pattern: page/pageSize query params, sql count + limit/offset"

requirements-completed: [AUTH-05, AUTH-06, AUTH-07, ADMIN-05, ADMIN-06, ADMIN-07]

duration: 6min
completed: 2026-04-25
---

# Phase 2.1 Plan 01: 后端 API 层 Summary

**Seed 迁移到 root + 角色注册 + 修改密码 + 管理员用户 CRUD 四端点**

## Performance

- **Duration:** 6 min
- **Started:** 2026-04-25T04:20:01Z
- **Completed:** 2026-04-25T04:26:42Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Seed 从 admin 迁移到 root（用户名 + 环境变量全部更新）
- 新增 registerUserWithRole 支持角色注册（STUDENT/ADMIN），拒绝 ROOT
- 新增 POST /api/auth/change-password 端点（验证旧密码后更新）
- 新增管理员用户管理 CRUD：列表/创建/切换状态/重置密码

## Task Commits

Each task was committed atomically:

1. **Task 1: Seed 迁移 + 注册 API role 支持** - `c0b6565` (feat)
2. **Task 2: 管理员用户管理 API** - `73efc8a` (feat)

## Files Created/Modified
- `packages/backend/src/routes/admin-users.ts` — 管理员用户管理 CRUD 路由（新建）
- `packages/backend/src/db/seed/admin.seed.ts` — ROOT_USERNAME/ROOT_PASSWORD/ROOT_DISPLAY_NAME 环境变量
- `packages/backend/src/services/auth.service.ts` — 新增 registerUserWithRole/changePassword/listUsers/createUserByAdmin/toggleUserStatus/resetUserPassword
- `packages/backend/src/routes/auth.ts` — 注册支持 role 参数 + display_name 可选 + change-password 端点
- `packages/backend/src/index.ts` — 挂载 admin-users 路由
- `packages/backend/src/__tests__/auth-register.test.ts` — 角色注册测试 + 修改密码测试
- `packages/backend/src/__tests__/seed-password.test.ts` — 环境变量引用更新

## Decisions Made
- registerUserWithRole 在服务层验证角色，拒绝 ROOT(100)，仅允许 STUDENT(1) 和 ADMIN(10)
- display_name 默认等于 username，降低注册门槛（per D-02）
- registerUser 保持向后兼容，内部调用 registerUserWithRole 并传 ROLE.STUDENT
- 管理员 CRUD 端点全部使用 AdminAuth（role >= 10），Phase 2.1 暂不做权限限制（per D-17）

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- bun test 运行器下，`vi.mock()` 可能跨文件污染 mock（password.test.ts 被 auth-register.test.ts 的 mock 影响）。单独运行时正常。这是 bun test 的已知行为，非代码问题。

## Next Phase Readiness
- 后端 API 全部就绪，前端 Plan 02（注册+登出+密码修改）和 Plan 03（管理端用户管理）可并行开发
- 所有新端点在 Scalar UI (/api/doc/ui) 可见可测试

## Self-Check: PASSED

- All 5 key files exist on disk
- Both task commits found: c0b6565, 73efc8a
- typecheck passes, tests pass

---
*Phase: 02.1-用户基础流程补齐*
*Completed: 2026-04-25*
