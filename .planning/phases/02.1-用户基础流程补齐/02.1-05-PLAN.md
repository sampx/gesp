---
phase: 02.1-用户基础流程补齐
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/register/page.tsx
  - apps/web/src/app/register/actions.ts
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Registration page only shows Student role card"
    - "Selecting the single role always succeeds (no backend rejection for teacher)"
  artifacts:
    - path: "apps/web/src/app/register/page.tsx"
      provides: "Registration page with student-only role card"
      contains: "student"
    - path: "apps/web/src/app/register/actions.ts"
      provides: "Registration action hardcoded to STUDENT role"
      contains: "ROLE.STUDENT"
  key_links:
    - from: "apps/web/src/app/register/page.tsx"
      to: "apps/web/src/app/register/actions.ts"
      via: "registerAction server action"
      pattern: "registerAction"
---

<objective>
Remove teacher role card from registration page and hardcode STUDENT role in registration action.

Purpose: Close UAT gap #2 (major) — frontend still shows teacher card but backend rejects teacher registration.
Output: Clean student-only registration page.
</objective>

<execution_context>
@/Users/sam/coding/wopal/wopal-workspace/.agents/wsf/workflows/execute-plan.md
@/Users/sam/coding/wopal/wopal-workspace/.agents/wsf/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-用户基础流程补齐/02.1-02-SUMMARY.md

<interfaces>
From apps/web/src/app/register/page.tsx:
```typescript
// Line 30: state type is "student" | "teacher"
// Lines 68-83: Two RoleCard components (student + teacher)
```

From apps/web/src/app/register/actions.ts:
```typescript
// Lines 17-21: roleMap includes teacher → ROLE.ADMIN mapping
// Line 21: const role = roleMap[roleStr] ?? ROLE.STUDENT;
```

From @gesp/shared:
```typescript
export const ROLE = { STUDENT: 1, ADMIN: 10, ROOT: 100 };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove teacher role card and hardcode STUDENT registration</name>
  <files>apps/web/src/app/register/page.tsx, apps/web/src/app/register/actions.ts</files>
  <action>
Simplify registration to student-only. User decided teacher self-registration is removed (per UAT test 3 feedback).

**1. register/page.tsx:**
- Remove the `selectedRole` state entirely (no longer needed with single role)
- Remove the `grid grid-cols-2 gap-3 mb-6` div containing BOTH RoleCards (lines 68-83)
- Replace with a simple static indicator: a div with className "mb-6 p-3 bg-secondary/50 rounded-lg text-center text-sm text-muted-foreground" showing "注册为学员" or similar
- Remove `formData.set("role", selectedRole)` from handleSubmit (line 53) — no need to set role since action hardcodes it
- Remove the `RoleCard` import (line 7) since it's no longer used

**2. register/actions.ts:**
- Remove the entire roleMap object (lines 17-20)
- Remove the roleStr extraction from formData (line 11)
- Hardcode role as ROLE.STUDENT in the fetch body: `body: JSON.stringify({ username, password, role: ROLE.STUDENT })`
- The redirect logic on lines 54-59 can be simplified — new student users always go to /student/dashboard
  </action>
  <verify>
    <automated>cd /Users/sam/coding/wopal/wopal-workspace/projects/gesp && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Registration page shows only student indicator, no teacher card; action hardcodes ROLE.STUDENT; no RoleCard import or selectedRole state</done>
</task>

</tasks>

<verification>
1. Navigate to /register — only student role indicator visible, no teacher card
2. Submit registration — succeeds and redirects to /student/dashboard
3. No TypeScript errors
</verification>

<success_criteria>
- Registration page shows single student role indicator
- No teacher role card or role selection UI
- Action hardcodes ROLE.STUDENT
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-用户基础流程补齐/02.1-05-SUMMARY.md`
</output>
