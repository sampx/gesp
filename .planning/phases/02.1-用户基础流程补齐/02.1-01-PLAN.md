---
phase: 02.1-用户基础流程补齐
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/db/seed/admin.seed.ts
  - packages/backend/src/services/auth.service.ts
  - packages/backend/src/routes/auth.ts
  - packages/backend/src/routes/admin-users.ts
  - packages/backend/src/index.ts
  - packages/backend/src/__tests__/auth-register.test.ts
autonomous: true
requirements:
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - ADMIN-05
  - ADMIN-06
  - ADMIN-07

must_haves:
  truths:
    - "Root seed 用户名为 root（不是 admin），环境变量使用 ROOT_* 前缀"
    - "注册 API 支持 role 参数（STUDENT/ADMIN），拒绝 ROOT 自助注册"
    - "所有已登录用户可修改密码（需验证旧密码）"
    - "管理员可添加学员和教员账号"
    - "管理员可查看用户列表、切换启用/禁用状态、重置密码"
    - "登出 API 已存在并正常工作"
  artifacts:
    - path: "packages/backend/src/routes/admin-users.ts"
      provides: "Admin user management API routes"
      exports: ["adminUsersRoutes"]
    - path: "packages/backend/src/routes/auth.ts"
      provides: "Updated register (role support) + change-password endpoint"
      min_lines: 80
    - path: "packages/backend/src/services/auth.service.ts"
      provides: "registerUser with role, changePassword, admin user operations"
      min_lines: 60
  key_links:
    - from: "packages/backend/src/index.ts"
      to: "packages/backend/src/routes/admin-users.ts"
      via: "app.route('/api/admin/users', adminUsersRoutes)"
      pattern: "app\\.route.*admin/users"
    - from: "packages/backend/src/routes/auth.ts"
      to: "packages/backend/src/services/auth.service.ts"
      via: "registerUser/registerUserWithRole/changePassword imports"
      pattern: "import.*auth\\.service"
    - from: "packages/backend/src/routes/admin-users.ts"
      to: "packages/backend/src/middleware/auth.ts"
      via: "AdminAuth middleware"
      pattern: "AdminAuth"
---

<objective>
创建 Phase 2.1 所需的全部后端 API，包括：seed 迁移（admin→root）、注册 API 支持 role 参数、修改密码 API、管理员用户管理 CRUD API。

Purpose: 前端所有页面依赖这些后端 API。后端完成后，前端两个 plan 可并行开发。
Output: 完整的后端 API 层，所有新增端点可通过 curl/Scalar UI 验证。
</objective>

<execution_context>
@/Users/sam/coding/wopal/wopal-workspace/.agents/wsf/workflows/execute-plan.md
@/Users/sam/coding/wopal/wopal-workspace/.agents/wsf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-用户基础流程补齐/02.1-CONTEXT.md

<interfaces>
<!-- Existing interfaces the executor MUST use -->

From packages/shared/src/constants/role.ts:
```typescript
export const ROLE = {
  STUDENT: 1,
  ADMIN: 10,
  ROOT: 100,
} as const;
export type RoleValue = typeof ROLE[keyof typeof ROLE];
```

From packages/shared/src/constants/status.ts:
```typescript
export const USER_STATUS = {
  ENABLED: 1,
  DISABLED: 2,
} as const;
```

From packages/backend/src/utils/response.ts:
```typescript
success(c, data?, message?)   // 200
error(c, message, status?)    // 400
unauthorized(c, message?)     // 401
forbidden(c, message?)        // 403
```

From packages/backend/src/middleware/auth.ts:
```typescript
AdminAuth()    // role >= 10
RootAuth()     // role >= 100
```

From packages/backend/src/middleware/session.ts:
```typescript
createSession(c, userId, role)   // 创建 session + 设 cookie
destroySession(c)                // 删除 session + 清 cookie
```

From packages/backend/src/utils/password.ts:
```typescript
hashPassword(password: string): Promise<string>
verifyPassword(password: string, hash: string): Promise<boolean>
```

From packages/backend/src/db/schema/users.ts:
```typescript
export const users = sqliteTable("users", {
  id, username, password_hash, display_name, role, status, email,
  github_id, oidc_id, wechat_id, telegram_id, created_at, updated_at
});
```

From packages/backend/AGENTS.md — New route template:
```typescript
const app = new Hono();
app.post("/xxx", AdminAuth(), zValidator("json", bodySchema), describeRoute({...}), async (c) => {
  return success(c, result);
});
export default app;
```
Mount pattern: `app.route("/api/xxx", xxxRoutes);`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed 迁移 + 注册 API role 支持</name>
  <files>
    packages/backend/src/db/seed/admin.seed.ts,
    packages/backend/src/services/auth.service.ts,
    packages/backend/src/routes/auth.ts,
    packages/backend/src/__tests__/auth-register.test.ts
  </files>
  <read_first>
    packages/backend/src/db/seed/admin.seed.ts
    packages/backend/src/services/auth.service.ts
    packages/backend/src/routes/auth.ts
    packages/backend/src/__tests__/auth-register.test.ts
    packages/shared/src/constants/role.ts
    packages/backend/AGENTS.md (日志规范 + 路由模板)
  </read_first>
  <behavior>
    - Test: registerUserWithRole("teacher1", "pass123", ROLE.ADMIN) creates user with role=10
    - Test: registerUserWithRole("student1", "pass123", ROLE.STUDENT) creates user with role=1
    - Test: registerUserWithRole("bad", "pass123", ROLE.ROOT) returns error "Registration not allowed for this role"
    - Test: registerUser("auto1", "pass123") still creates student (backward compat, display_name defaults to username)
    - Test: changePassword with correct old password succeeds
    - Test: changePassword with wrong old password returns error
  </behavior>
  <action>
    **1. Seed 迁移（per D-18）：**

    修改 `packages/backend/src/db/seed/admin.seed.ts`：
    - 环境变量名从 `ADMIN_PASSWORD` → `ROOT_PASSWORD`（`process.env.ROOT_PASSWORD`）
    - 环境变量名从 `ADMIN_USERNAME` → `ROOT_USERNAME`（`process.env.ROOT_USERNAME`）
    - 环境变量名从 `ADMIN_DISPLAY_NAME` → `ROOT_DISPLAY_NAME`（`process.env.ROOT_DISPLAY_NAME`）
    - 默认用户名从 `"admin"` → `"root"`
    - 默认 display_name 从 `"System Admin"` → `"超级管理员"`
    - 默认密码从 `"admin123"` → `"root123"`
    - 生产环境检查错误消息从 `ADMIN_PASSWORD` → `ROOT_PASSWORD`
    - logger 消息中的 `seed_admin` → `seed_root`，`"Root admin seeded"` → `"Root user seeded"`

    **2. 注册服务更新（per D-02, D-03, D-04, D-15）：**

    修改 `packages/backend/src/services/auth.service.ts`：
    - 新增 `registerUserWithRole(username, password, role)` 函数：
      - 验证 role 只能是 ROLE.STUDENT(1) 或 ROLE.ADMIN(10)，否则返回 `{ success: false, error: "Registration not allowed for this role" }`
      - display_name 默认设为 username（per D-02）
      - 其他逻辑与现有 registerUser 一致（密码 ≥6、检查用户名唯一、bcrypt 哈希、insert）
    - 保留原有 `registerUser` 函数向后兼容（内部调用 registerUserWithRole 并传 ROLE.STUDENT）

    新增 `changePassword(userId, oldPassword, newPassword)` 函数：
    - 通过 userId 查询用户
    - 用 verifyPassword 验证旧密码，不匹配返回 `{ success: false, error: "旧密码不正确" }`
    - 新密码长度 ≥6
    - 用 hashPassword 更新 password_hash，同时更新 updated_at

    **3. 注册路由更新（per D-04）：**

    修改 `packages/backend/src/routes/auth.ts`：
    - registerBodySchema 新增可选 `role` 字段：`role: z.number().int().optional()`
    - registerBodySchema 中 `display_name` 改为可选：`display_name: z.string().min(1).max(100).optional()`
    - handler 中：如果提供了 role，调用 `registerUserWithRole`，否则调用 `registerUser`
    - display_name 默认值：`display_name ?? username`

    新增 change-password 路由：
    ```
    app.post("/change-password", validateSession, ...)
    ```
    - body schema: `{ old_password: z.string(), new_password: z.string().min(6) }`
    - handler 调用 changePassword service
    - 成功返回 `success(c, undefined, "Password changed successfully")`
    - 失败返回 `error(c, result.error)`

    **4. 更新测试：**

    修改 `packages/backend/src/__tests__/auth-register.test.ts`：
    - 更新环境变量引用：`ADMIN_PASSWORD` → `ROOT_PASSWORD`
    - 新增 registerUserWithRole 测试用例（teacher 角色注册、ROOT 角色拒绝）
    - 新增 changePassword 测试用例（正确旧密码、错误旧密码）
  </action>
  <verify>
    <automated>cd packages/backend && bun test</automated>
  </verify>
  <done>
    - Seed 使用 ROOT_USERNAME/ROOT_PASSWORD/ROOT_DISPLAY_NAME 环境变量，默认用户名 root
    - registerUserWithRole 支持 STUDENT(1) 和 ADMIN(10) 角色，拒绝 ROOT(100)
    - display_name 默认等于 username
    - POST /api/auth/change-password 验证旧密码后更新密码
    - 所有测试通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 管理员用户管理 API</name>
  <files>
    packages/backend/src/routes/admin-users.ts,
    packages/backend/src/services/auth.service.ts,
    packages/backend/src/index.ts
  </files>
  <read_first>
    packages/backend/src/services/auth.service.ts
    packages/backend/src/routes/auth.ts
    packages/backend/src/index.ts
    packages/backend/src/middleware/auth.ts
    packages/backend/src/utils/response.ts
    packages/backend/AGENTS.md (路由模板 + 日志规范)
  </read_first>
  <action>
    **1. 新增 service 函数（追加到 auth.service.ts）：**

    ```typescript
    // 获取用户列表（分页）
    export async function listUsers(options?: { page?: number; pageSize?: number })
    ```
    - 返回所有用户（不含 password_hash），按 created_at 降序
    - 分页参数默认 page=1, pageSize=20
    - 使用 `db.select({ id, username, display_name, role, status, created_at }).from(users).orderBy(desc(users.created_at)).limit().offset()`

    ```typescript
    // 管理员添加用户
    export async function createUserByAdmin(username: string, password: string, role: number)
    ```
    - role 只接受 STUDENT(1) 或 ADMIN(10)，拒绝 ROOT(100)
    - display_name 默认设为 username
    - 检查用户名唯一性
    - 密码 bcrypt 哈希
    - 返回新用户（不含 password_hash）

    ```typescript
    // 切换用户状态
    export async function toggleUserStatus(userId: string)
    ```
    - 查询当前状态，反转：ENABLED↔DISABLED
    - 更新 status 和 updated_at

    ```typescript
    // 重置密码
    export async function resetUserPassword(userId: string, newPassword: string)
    ```
    - 验证 newPassword ≥6
    - 更新 password_hash 和 updated_at

    **2. 新建路由文件 `packages/backend/src/routes/admin-users.ts`：**

    遵循 backend/AGENTS.md 路由模板模式：
    ```typescript
    import { Hono } from "hono";
    import { describeRoute, resolver } from "hono-openapi";
    import { z } from "zod";
    import { zValidator } from "@hono/zod-validator";
    import { AdminAuth } from "../middleware/auth";
    import { listUsers, createUserByAdmin, toggleUserStatus, resetUserPassword } from "../services/auth.service";
    import { success, error, forbidden } from "../utils/response";

    const app = new Hono();
    ```

    端点（全部使用 `AdminAuth()` 中间件，per D-17 Phase 2.1 不做权限限制）：

    `GET /` — 用户列表
    - query schema: `{ page?: z.coerce.number().int().positive().default(1), pageSize?: z.coerce.number().int().positive().max(100).default(20) }`
    - 调用 listUsers，返回 `{ users: [...], total: number, page, pageSize }`

    `POST /` — 添加用户（per ADMIN-06, ADMIN-07）
    - body schema: `{ username: z.string().min(3).max(20), password: z.string().min(6), role: z.number().int().refine(v => v === 1 || v === 10, "Invalid role") }`
    - 调用 createUserByAdmin
    - 成功返回 201 `success(c, user, "User created")`
    - 失败返回 `error(c, result.error)`

    `PUT /:id/status` — 切换状态（per ADMIN-05）
    - param schema: `{ id: z.string() }`
    - 调用 toggleUserStatus
    - 成功返回 `success(c, { status: newStatus }, "User status updated")`

    `POST /:id/reset-password` — 重置密码（per ADMIN-05）
    - body schema: `{ new_password: z.string().min(6) }`
    - 调用 resetUserPassword
    - 成功返回 `success(c, undefined, "Password reset")`

    每个端点使用 `describeRoute()` 添加 OpenAPI 描述。

    **3. 挂载路由（修改 packages/backend/src/index.ts）：**

    在 auth routes 下方添加：
    ```typescript
    import adminUsersRoutes from "./routes/admin-users";
    // ...
    app.route("/api/admin/users", adminUsersRoutes);
    ```
  </action>
  <verify>
    <automated>cd packages/backend && bun run typecheck && bun test</automated>
  </verify>
  <done>
    - `GET /api/admin/users` 返回用户列表（分页，不含 password_hash）
    - `POST /api/admin/users` 可添加学员(role=1)和教员(role=10)
    - `PUT /api/admin/users/:id/status` 切换启用/禁用
    - `POST /api/admin/users/:id/reset-password` 重置用户密码
    - 所有端点需要 AdminAuth (role >= 10)
    - 路由已挂载到 index.ts
    - typecheck 通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted input on all endpoints |
| Register form → Backend | User-controlled role field (must not allow ROOT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | S (Spoofing) | POST /register | mitigate | validate role field: reject ROOT(100), only allow STUDENT(1) and ADMIN(10) |
| T-02.1-02 | T (Tampering) | PUT /admin/users/:id/status | mitigate | AdminAuth middleware ensures only role >= 10 can toggle status |
| T-02.1-03 | I (Info Disclosure) | GET /admin/users | mitigate | Exclude password_hash from all query results; AdminAuth gate |
| T-02.1-04 | E (Elevation) | POST /admin/users | mitigate | Reject role=ROOT in createUserByAdmin; AdminAuth gate on all admin endpoints |
| T-02.1-05 | T (Tampering) | POST /auth/change-password | mitigate | Verify old password before allowing change; validateSession ensures authenticated |
</threat_model>

<verification>
1. `cd packages/backend && bun test` — 所有测试通过
2. `cd packages/backend && bun run typecheck` — 类型检查通过
3. 启动 `bun run dev`，通过 Scalar UI (/api/doc/ui) 验证所有新端点出现
4. 手动 curl 测试：注册教员 → 登录 → 修改密码 → 管理员添加用户 → 列表 → 切换状态 → 重置密码
</verification>

<success_criteria>
- Seed 默认用户名为 root，使用 ROOT_* 环境变量
- 注册 API 支持 role 参数（STUDENT/ADMIN），ROOT 角色被拒绝
- display_name 默认等于 username
- POST /api/auth/change-password 正常工作
- GET/POST/PUT /api/admin/users/* 四个端点全部可用
- 所有端点有 AdminAuth 或 validateSession 保护
- 密码不在任何 API 响应中返回
- 所有测试通过 + typecheck 通过
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-用户基础流程补齐/02.1-01-SUMMARY.md`
</output>
