---
phase: 02.1-用户基础流程补齐
plan: 02
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - apps/web/src/app/register/page.tsx
  - apps/web/src/app/register/actions.ts
  - apps/web/src/app/login/page.tsx
  - apps/web/src/middleware.ts
  - apps/web/src/app/student/layout.tsx
  - apps/web/src/components/student-navbar.tsx
  - apps/web/src/components/admin-sidebar.tsx
autonomous: true
requirements:
  - AUTH-05
  - UI-AUTH-01
  - UI-AUTH-02

must_haves:
  truths:
    - "学员/教员可通过 /register 页面自助注册，注册成功自动登录跳转对应首页"
    - "登录页底部有「没有账号？去注册」链接"
    - "注册页底部有「已有账号？去登录」链接"
    - "学员端导航有用户头像+下拉菜单（设置、登出）"
    - "管理端侧边栏底部有用户信息+登出按钮"
    - "登出后清除会话跳转登录页"
  artifacts:
    - path: "apps/web/src/app/register/page.tsx"
      provides: "Registration page component"
      min_lines: 60
    - path: "apps/web/src/app/register/actions.ts"
      provides: "Register server action"
      min_lines: 30
    - path: "apps/web/src/components/student-navbar.tsx"
      provides: "Student top navigation with avatar dropdown"
      min_lines: 40
    - path: "apps/web/src/components/admin-sidebar.tsx"
      provides: "Admin sidebar with bottom user area"
      min_lines: 80
  key_links:
    - from: "apps/web/src/app/register/actions.ts"
      to: "http://localhost:3000/api/auth/register"
      via: "fetch POST"
      pattern: "fetch.*api/auth/register"
    - from: "apps/web/src/components/student-navbar.tsx"
      to: "http://localhost:3000/api/auth/logout"
      via: "server action logout"
      pattern: "logoutAction|api/auth/logout"
    - from: "apps/web/src/middleware.ts"
      to: "/register"
      via: "PUBLIC_PATHS array"
      pattern: "PUBLIC_PATHS.*register"
---

<objective>
创建注册页面、更新登录页面（添加注册链接）、添加学员端 Navbar 和管理端侧边栏用户区域（含登出功能），完成注册→登录→使用→登出的前端闭环。

Purpose: 实现用户从注册到登出的完整前端流程，与 Plan 01 后端 API 配合。
Output: 可用的注册页、更新的登录页、学员端 Navbar、管理端侧边栏用户区。
</objective>

<execution_context>
@/Users/sam/coding/wopal/wopal-workspace/.agents/wsf/workflows/execute-plan.md
@/Users/sam/coding/wopal/wopal-workspace/.agents/wsf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.1-用户基础流程补齐/02.1-CONTEXT.md
@.planning/phases/02.1-用户基础流程补齐/02.1-UI-SPEC.md
@.planning/phases/02.1-用户基础流程补齐/02.1-01-SUMMARY.md

<interfaces>
<!-- Backend API contracts from Plan 01 -->

POST /api/auth/register
  Request:  { username: string (3-50), password: string (6+), role?: number (1|10), display_name?: string }
  Response: { success: true, data: { user: { id, username, display_name, role } }, message: "Registration successful" }
  Cookie:   Set-Cookie: session_id=...
  Notes:    role 默认 1(STUDENT)。role=100(ROOT) 被拒绝。

POST /api/auth/login
  Request:  { username: string, password: string }
  Response: { success: true, data: { user: { id, username, display_name, role } } }
  Cookie:   Set-Cookie: session_id=...

POST /api/auth/logout
  Cookie:   Clears session_id
  Response: { success: true, message: "Logout successful" }

GET /api/auth/me
  Cookie:   session_id=...
  Response: { success: true, data: { user: { id, username, display_name, role, status, email } } }

<!-- Existing frontend interfaces -->

From apps/web/src/components/role-card.tsx:
```typescript
interface RoleCardProps {
  role: "student" | "teacher" | "admin";
  emoji: string;
  label: string;
  selected: boolean;
  onClick: () => void;
}
export function RoleCard(props: RoleCardProps): JSX.Element;
```

From apps/web/src/app/login/actions.ts (server action pattern):
```typescript
"use server";
import { cookies } from "next/headers";
import { redirect } from "next/navigation";
// fetch backend → extract Set-Cookie → set cookie → redirect
```

From apps/web/src/middleware.ts:
```typescript
const PUBLIC_PATHS = ["/login", "/_next", "/favicon.ico"];
```

From packages/shared/src/constants/role.ts:
```typescript
export const ROLE = { STUDENT: 1, ADMIN: 10, ROOT: 100 } as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 注册页面 + 登录页更新 + 路由中间件</name>
  <files>
    apps/web/src/app/register/page.tsx,
    apps/web/src/app/register/actions.ts,
    apps/web/src/app/login/page.tsx,
    apps/web/src/middleware.ts
  </files>
  <read_first>
    apps/web/src/app/login/page.tsx
    apps/web/src/app/login/actions.ts
    apps/web/src/components/role-card.tsx
    apps/web/src/middleware.ts
    apps/web/src/app/register/page.tsx (if exists)
  </read_first>
  <acceptance_criteria>
    - 文件 apps/web/src/app/register/page.tsx 存在且包含 "创建账号" 标题
    - 文件包含 RoleCard 组件引用且只有 "student" 和 "teacher" 两个角色卡（无 admin）
    - 文件包含 Link href="/login" 且文字为 "已有账号？去登录"
    - 文件 apps/web/src/app/register/actions.ts 包含 "use server" 和 fetch api/auth/register
    - apps/web/src/app/login/page.tsx 包含 Link href="/register" 且文字为 "没有账号？去注册"
    - apps/web/src/middleware.ts 中 PUBLIC_PATHS 包含 "/register"
  </acceptance_criteria>
  <action>
    **1. 创建 `apps/web/src/app/register/actions.ts`：**

    ```typescript
    "use server";

    import { cookies } from "next/headers";
    import { redirect } from "next/navigation";
    import { ROLE } from "@gesp/shared";

    export async function registerAction(formData: FormData) {
      const username = formData.get("username") as string;
      const password = formData.get("password") as string;
      const roleStr = formData.get("role") as string;

      // Map frontend role string to backend role number
      const roleMap: Record<string, number> = {
        student: ROLE.STUDENT,
        teacher: ROLE.ADMIN,
      };
      const role = roleMap[roleStr] ?? ROLE.STUDENT;

      const backendUrl = process.env.BACKEND_URL || "http://localhost:3000";

      let redirectPath = "";
      try {
        const res = await fetch(`${backendUrl}/api/auth/register`, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ username, password, role }),
        });

        const data = await res.json();

        if (!res.ok) {
          return { error: data.message || "注册失败，请稍后重试" };
        }

        // Extract session_id from Set-Cookie (same pattern as login)
        const rawCookie = res.headers.getSetCookie?.() || [];
        const sessionCookie = rawCookie.find((c: string) => c.startsWith("session_id="));
        if (sessionCookie) {
          const cookieStore = await cookies();
          const sessionId = sessionCookie.split(";")[0].split("=")[1];
          cookieStore.set("session_id", sessionId, {
            path: "/",
            httpOnly: true,
            sameSite: "strict",
            maxAge: 7 * 24 * 60 * 60,
          });
        }

        // Role-based redirect (per D-03)
        const userRole = data.data?.user?.role;
        if (userRole >= ROLE.ADMIN) {
          redirectPath = "/admin/dashboard";
        } else {
          redirectPath = "/student/dashboard";
        }
      } catch {
        return { error: "注册失败，请稍后重试" };
      }

      if (redirectPath) redirect(redirectPath);
    }
    ```

    **2. 创建 `apps/web/src/app/register/page.tsx`（per D-01, D-02, D-04）：**

    延续登录页的视觉风格。使用 `"use client"` 因为需要 useState 管理 role 选中状态。

    结构：
    - 居中 Card（max-w-md），与登录页一致
    - CardHeader: title="创建账号", description="选择角色并填写信息注册"
    - 两张角色卡：学员 🎮 / 教员 📚（grid-cols-2），**不包含管理员**（per D-04）
    - 表单：用户名 + 密码 + 确认密码（per D-02）
    - 客户端验证（Zod schema 或手动）：密码 ≥6、确认密码匹配
    - 提交按钮 "注册"
    - 底部链接 "已有账号？去登录" → `/login`（per D-01）
    - 背景：与登录页相同的 `bg-[var(--background)]` warm student theme

    复用 `RoleCard` 组件（import from `@/components/role-card`）。
    复用 `SubmitButton` 模式（useFormStatus 显示加载状态）。

    确认密码客户端验证：在 handleSubmit 中检查 password === confirmPassword，不匹配则 `toast.error("两次输入的密码不一致")`。

    **3. 修改 `apps/web/src/app/login/page.tsx`（per D-01）：**

    在表单底部 `<p>` 标签之前（submit 按钮之后），添加：
    ```tsx
    <div className="text-center text-sm">
      <Link href="/register" className="text-primary hover:underline">
        没有账号？去注册
      </Link>
    </div>
    ```

    需要新增 `import Link from "next/link";`

    **4. 修改 `apps/web/src/middleware.ts`：**

    将 `/register` 添加到 PUBLIC_PATHS：
    ```typescript
    const PUBLIC_PATHS = ["/login", "/register", "/_next", "/favicon.ico"];
    ```
  </action>
  <verify>
    <automated>cd apps/web && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - /register 页面展示学员和教员两个角色卡（无管理员）
    - 注册表单包含用户名、密码、确认密码三个字段
    - 注册成功后自动登录并跳转对应首页
    - 注册页底部有"已有账号？去登录"链接
    - 登录页底部有"没有账号？去注册"链接
    - /register 在 PUBLIC_PATHS 中，无需登录即可访问
    - typecheck 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 学员 Navbar + 管理端侧边栏用户区 + 登出</name>
  <files>
    apps/web/src/app/student/layout.tsx,
    apps/web/src/components/student-navbar.tsx,
    apps/web/src/components/admin-sidebar.tsx
  </files>
  <read_first>
    apps/web/src/app/student/layout.tsx
    apps/web/src/components/admin-sidebar.tsx
    apps/web/src/app/login/actions.ts (server action pattern)
    packages/shared/src/constants/role.ts
    apps/web/src/components/ui/dropdown-menu.tsx
    apps/web/src/components/ui/avatar.tsx
    apps/web/src/components/ui/separator.tsx
    apps/web/src/components/ui/badge.tsx
  </read_first>
  <acceptance_criteria>
    - apps/web/src/components/student-navbar.tsx 存在且 export StudentNavbar
    - 文件包含 DropdownMenu 组件使用
    - 文件包含 "登出" 文字
    - 文件包含 Link href="/student/settings"
    - apps/web/src/app/student/layout.tsx import StudentNavbar
    - apps/web/src/components/admin-sidebar.tsx 包含 "登出" 按钮
    - admin-sidebar.tsx 包含 Avatar 组件
    - admin-sidebar.tsx 包含 Separator 组件
  </acceptance_criteria>
  <action>
    **1. 创建 `apps/web/src/components/student-navbar.tsx`（per D-05, D-07）：**

    "use client" 组件。使用 TanStack Query 的 useQuery 获取当前用户信息（GET /api/auth/me），并展示在导航栏。

    结构（per UI-SPEC）：
    ```
    ┌──────────────────────────────────────────────┐
    │ [GESP 智能学习]              [👤 张三 ▾]      │
    ├──────────────────────────────────────────────┤
    ```

    详细实现：
    - 外层 `<header>` 固定在顶部：`h-14 flex items-center justify-between px-4 border-b bg-card`
    - 左侧：`<span className="font-semibold text-base">GESP 智能学习</span>`
    - 右侧：Avatar 组件 + display_name + DropdownMenu
      - Avatar：取 display_name 首字符，32px circle `<Avatar className="h-8 w-8">`
      - display_name：`<span className="text-sm hidden sm:inline">{user.display_name}</span>`（mobile 隐藏 per UI-SPEC）
      - DropdownMenu trigger：ChevronDown 图标
    - DropdownMenu items：
      1. "⚙️ 设置" → `/student/settings`（用 `Link` 包裹 `DropdownMenuItem`）
      2. Separator
      3. "🚪 登出" → 触发登出 action

    登出逻辑（per D-07，无确认弹窗）：
    - 创建内联 server action 或直接 fetch POST /api/auth/logout
    - 因为这是 client component，使用直接 fetch + redirect：
    ```typescript
    async function handleLogout() {
      const backendUrl = process.env.NEXT_PUBLIC_BACKEND_URL || "http://localhost:3000";
      await fetch(`${backendUrl}/api/auth/logout`, {
        method: "POST",
        credentials: "include",
      });
      // 清除 cookie 并跳转
      document.cookie = "session_id=; path=/; expires=Thu, 01 Jan 1970 00:00:00 GMT";
      window.location.href = "/login";
    }
    ```

    用户数据获取：使用 fetch GET /api/auth/me（携带 cookie），如果 401 则不显示用户信息区。

    注意：前端环境变量需要 NEXT_PUBLIC_ 前缀才能在 client 组件中使用。当前 actions.ts 使用的是 server-side `process.env.BACKEND_URL`。对于 Navbar 这类 client 组件，使用 `NEXT_PUBLIC_BACKEND_URL` 或回退到 `http://localhost:3000`。

    **2. 修改 `apps/web/src/app/student/layout.tsx`（per D-05）：**

    引入 StudentNavbar：
    ```tsx
    import { StudentNavbar } from "@/components/student-navbar";
    import { ReactNode } from "react";

    export default function StudentLayout({ children }: { children: ReactNode }) {
      return (
        <div data-theme="student" className="min-h-screen font-sans">
          <StudentNavbar />
          <div className="max-w-3xl mx-auto px-4 pb-20">
            {children}
          </div>
        </div>
      );
    }
    ```

    **3. 修改 `apps/web/src/components/admin-sidebar.tsx`（per D-06, D-07）：**

    在现有 sidebar 的 `<nav>` 和 `</aside>` 之间，添加底部用户区域。

    使用 TanStack Query 或直接 fetch 获取当前用户信息。考虑到 admin-sidebar 已是 "use client" 组件，需要一种方式获取当前用户。

    方案：在组件内部使用 `useEffect` + `fetch('/api/auth/me')` 获取用户信息，存储在 `useState`。如果请求失败（未登录），用户区域不显示（但 middleware 会拦截未登录用户，所以这里一定能拿到数据）。

    底部用户区实现（per UI-SPEC）：
    ```tsx
    {/* 底部用户区域 - mt-auto 固定在底部 */}
    <div className="mt-auto border-t p-3">
      {!collapsed ? (
        <div className="space-y-2">
          <div className="flex items-center gap-2">
            <Avatar className="h-7 w-7">
              <AvatarFallback>{user?.display_name?.[0] || "?"}</AvatarFallback>
            </Avatar>
            <span className="text-sm font-semibold truncate">{user?.username}</span>
          </div>
          <Badge variant="secondary" className="text-xs">
            {user?.role === 100 ? "超级管理员" : user?.role === 10 ? "教员" : "学员"}
          </Badge>
          <Button
            variant="ghost"
            size="sm"
            className="w-full text-destructive hover:text-destructive"
            onClick={handleLogout}
          >
            <LogOut className="h-4 w-4 mr-2" />
            登出
          </Button>
        </div>
      ) : (
        <div className="flex flex-col items-center gap-2">
          <Avatar className="h-7 w-7">
            <AvatarFallback>{user?.display_name?.[0] || "?"}</AvatarFallback>
          </Avatar>
          <Button variant="ghost" size="icon" onClick={handleLogout}>
            <LogOut className="h-4 w-4" />
          </Button>
        </div>
      )}
    </div>
    ```

    需要新增 imports：
    - `import { Avatar, AvatarFallback } from "@/components/ui/avatar";`
    - `import { Badge } from "@/components/ui/badge";`
    - `import { LogOut } from "lucide-react";`
    - `import { useEffect, useState } from "react";`

    需要在 `<aside>` 上添加 `className` 使 flex 布局包含 `flex-col` 且 `min-h-screen`，确保底部用户区域始终可见：
    ```
    className={cn(
      "border-r bg-card flex flex-col min-h-screen transition-all duration-300",
      collapsed ? "w-16" : "w-60"
    )}
    ```

    登出逻辑与 StudentNavbar 相同。
  </action>
  <verify>
    <automated>cd apps/web && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - StudentNavbar 组件包含系统名称、用户头像、下拉菜单（设置+登出）
    - student/layout.tsx 引入 StudentNavbar
    - admin-sidebar.tsx 底部有 Avatar + 用户名 + 角色标签 + 登出按钮
    - 登出点击即执行（无确认弹窗），清除会话跳转 /login
    - sidebar 折叠时底部只显示 Avatar + 登出图标
    - typecheck 通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/auth/register | User-controlled input, role field must not allow ROOT |
| Client → /api/auth/logout | Session cookie manipulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-06 | S (Spoofing) | register/actions.ts | mitigate | Frontend only sends role=1 or role=10; backend also validates (defense in depth) |
| T-02.1-07 | T (Tampering) | student-navbar logout | accept | Logout clears cookie client-side + calls backend; no sensitive data at risk |
</threat_model>

<verification>
1. `cd apps/web && bun run typecheck` — 类型检查通过
2. 手动流程验证：
   - 访问 /register → 选择学员 → 填写信息 → 注册 → 自动跳转 /student/dashboard
   - /student/dashboard 页面顶部应显示 StudentNavbar
   - 点击头像下拉 → 设置 → 跳转 /student/settings
   - 点击头像下拉 → 登出 → 跳转 /login
   - 登录 admin → 管理端侧边栏底部有用户信息和登出
</verification>

<success_criteria>
- 注册页面可用，支持学员和教员两个角色
- 注册成功自动登录跳转对应首页
- 登录页和注册页互相有链接
- 学员端 Navbar 显示用户信息+下拉菜单（设置/登出）
- 管理端侧边栏底部显示用户信息+登出按钮
- 登出后清除会话跳转登录页
- 所有 typecheck 通过
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-用户基础流程补齐/02.1-02-SUMMARY.md`
</output>
