---
phase: 02.1-用户基础流程补齐
verified: 2026-04-25T12:51:47Z
status: human_needed
score: 8/8 must-haves verified
overrides_applied: 0
re_verification: false
human_verification:
  - test: "注册完整流程：选择学员角色 → 填写用户名/密码/确认密码 → 注册 → 自动跳转学员端首页"
    expected: "注册成功后自动登录并跳转到 /student/dashboard"
    why_human: "需要启动前后端服务器验证跨域 cookie 设置和自动跳转行为"
  - test: "注册完整流程：选择教员角色 → 填写信息 → 注册 → 自动跳转管理端首页"
    expected: "注册成功后自动登录并跳转到 /admin/dashboard"
    why_human: "需要验证教员角色注册后的跳转路径正确性"
  - test: "学员端登出流程：点击头像下拉菜单 → 登出 → 跳转登录页"
    expected: "清除会话 cookie 并跳转到 /login"
    why_human: "需要验证 cookie 清除和重定向的完整行为"
  - test: "管理端登出流程：侧边栏底部 → 登出按钮 → 跳转登录页"
    expected: "清除会话 cookie 并跳转到 /login"
    why_human: "需要验证管理端侧边栏折叠/展开时登出按钮均可用"
  - test: "学员修改密码：访问 /student/settings → 输入旧密码/新密码/确认新密码 → 提交"
    expected: "显示密码修改成功 toast，保持登录状态"
    why_human: "需要验证跨端口 cookie 传递（NextJS→Hono 后端）的实际效果"
  - test: "管理员用户管理：访问 /admin/students → 添加学员 → 禁用 → 启用 → 重置密码"
    expected: "表格实时刷新，toast 反馈，状态切换正确显示"
    why_human: "需要验证多个 Dialog 交互和表格刷新的完整用户体验"
---

# Phase 2.1: 用户基础流程补齐 Verification Report

**Phase Goal:** 补齐注册→登录→使用→登出完整用户流程，管理员可管理教员和学员账号，使系统具备基本可用性
**Verified:** 2026-04-25T12:51:47Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | 学员可通过 /register 页面自助注册（用户名+密码+确认密码），注册成功自动登录跳转学员端首页 | ✓ VERIFIED | `register/page.tsx`(131 lines): 3-field form + RoleCard(student/teacher) + client validation. `register/actions.ts`(65 lines): "use server" fetch POST `/api/auth/register` → extract Set-Cookie → set session_id → redirect `/student/dashboard` or `/admin/dashboard` based on role |
| 2 | 登录页有"去注册"链接，注册页有"去登录"链接 | ✓ VERIFIED | `login/page.tsx` L92-94: `<Link href="/register">没有账号？去注册</Link>`. `register/page.tsx` L122-125: `<Link href="/login">已有账号？去登录</Link>` |
| 3 | 学员端导航有登出按钮，点击后清除会话跳转登录页 | ✓ VERIFIED | `student-navbar.tsx`(87 lines): DropdownMenu with `登出` MenuItem → `handleLogout()` calls `POST /api/auth/logout` + clears `document.cookie` + `window.location.href = "/login"`. Imported in `student/layout.tsx` |
| 4 | 管理端导航有登出按钮，点击后清除会话跳转登录页 | ✓ VERIFIED | `admin-sidebar.tsx`(155 lines): Bottom user area with `登出` Button (both collapsed/expanded states) → same `handleLogout()` pattern |
| 5 | 学员可通过 /student/settings 修改密码（旧密码+新密码+确认新密码） | ✓ VERIFIED | `student/settings/page.tsx` imports `PasswordChangeForm` which has 3 fields (old_password/new_password/confirm) + fetch `POST /api/auth/change-password`. Backend `auth.ts` L98-144: `validateSession` + `changePassword` service verifies old password |
| 6 | 管理员可通过管理端界面添加教员账号（用户名+密码） | ✓ VERIFIED | `add-user-dialog.tsx`(164 lines): Select with `value="10"` for 教员 → `POST /api/admin/users` with `{username, password, role: 10}`. Backend `admin-users.ts` L114-154: AdminAuth + createUserByAdmin validates role |
| 7 | 管理员可通过管理端界面添加学员账号（用户名+密码） | ✓ VERIFIED | Same `add-user-dialog.tsx`: Select with `value="1"` for 学员 → same API endpoint. Backend validates role=1 |
| 8 | 管理员可查看学员列表，支持禁用/启用账号和重置密码 | ✓ VERIFIED | `admin/students/page.tsx`(226 lines): Table (username/role/status/created_at/operations) + `toggleStatus()` calls `PUT /api/admin/users/:id/status` + `ResetPasswordDialog` calls `POST /api/admin/users/:id/reset-password`. Both backend endpoints use AdminAuth middleware |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/backend/src/routes/admin-users.ts` | Admin user management API routes | ✓ VERIFIED | 256 lines, exports `default app` with GET/POST/PUT/POST endpoints |
| `packages/backend/src/routes/auth.ts` | Updated register + change-password | ✓ VERIFIED | 293 lines, register with role support + change-password with validateSession |
| `packages/backend/src/services/auth.service.ts` | User ops service layer | ✓ VERIFIED | 229 lines, registerUserWithRole/changePassword/listUsers/createUserByAdmin/toggleUserStatus/resetUserPassword |
| `packages/backend/src/db/seed/admin.seed.ts` | Root seed migration | ✓ VERIFIED | 55 lines, ROOT_USERNAME/ROOT_PASSWORD/ROOT_DISPLAY_NAME env vars, default username "root" |
| `apps/web/src/app/register/page.tsx` | Registration page | ✓ VERIFIED | 131 lines, RoleCard + 3-field form + Link to /login |
| `apps/web/src/app/register/actions.ts` | Register server action | ✓ VERIFIED | 65 lines, "use server" + fetch + cookie + redirect |
| `apps/web/src/components/student-navbar.tsx` | Student navbar with dropdown | ✓ VERIFIED | 87 lines, Avatar + DropdownMenu + logout |
| `apps/web/src/components/admin-sidebar.tsx` | Admin sidebar with user area | ✓ VERIFIED | 155 lines, bottom user area with Avatar + Badge + logout in both collapsed/expanded |
| `apps/web/src/app/login/page.tsx` | Login with register link | ✓ VERIFIED | 104 lines, Link to /register added |
| `apps/web/src/middleware.ts` | Public routes include /register | ✓ VERIFIED | L5: PUBLIC_PATHS includes "/register" |
| `apps/web/src/components/password-change-form.tsx` | Shared password form | ✓ VERIFIED | 111 lines, 3 fields + fetch change-password + toast |
| `apps/web/src/app/student/settings/page.tsx` | Student settings | ✓ VERIFIED | 20 lines, imports PasswordChangeForm |
| `apps/web/src/app/admin/settings/page.tsx` | Admin settings + placeholder | ✓ VERIFIED | 28 lines, PasswordChangeForm + "系统配置功能将在后续版本上线" |
| `apps/web/src/app/admin/students/page.tsx` | Admin user management | ✓ VERIFIED | 226 lines, Table + AddUserDialog + ResetPasswordDialog |
| `apps/web/src/components/add-user-dialog.tsx` | Add user dialog | ✓ VERIFIED | 164 lines, role Select (学员/教员) + form + fetch POST |
| `apps/web/src/components/reset-password-dialog.tsx` | Reset password dialog | ✓ VERIFIED | 128 lines, new password + confirm + fetch POST |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `index.ts` | `admin-users.ts` | `app.route("/api/admin/users", adminUsersRoutes)` | ✓ WIRED | L7 import + L85 mount |
| `auth.ts` (routes) | `auth.service.ts` | `registerUser/registerUserWithRole/changePassword` | ✓ WIRED | L5 imports, called in handlers L68-72, L136 |
| `admin-users.ts` | `auth.ts` (middleware) | `AdminAuth` | ✓ WIRED | L5 import, applied to all 4 endpoints |
| `register/actions.ts` | `/api/auth/register` | `fetch POST` | ✓ WIRED | L27: fetch with session cookie extraction |
| `student-navbar.tsx` | `/api/auth/logout` | `fetch POST` | ✓ WIRED | L25: handleLogout function |
| `student-navbar.tsx` | `/api/auth/me` | `fetch GET` | ✓ WIRED | L40: useEffect fetch for user data |
| `admin-sidebar.tsx` | `/api/auth/logout` | `fetch POST` | ✓ WIRED | L45: handleLogout function |
| `admin-sidebar.tsx` | `/api/auth/me` | `fetch GET` | ✓ WIRED | L62: useEffect fetch for user data |
| `password-change-form.tsx` | `/api/auth/change-password` | `fetch POST` | ✓ WIRED | L42: fetch with cookie from document.cookie |
| `admin/students/page.tsx` | `/api/admin/users` | `fetch GET` | ✓ WIRED | L76: fetchUsers in useEffect |
| `add-user-dialog.tsx` | `/api/admin/users` | `fetch POST` | ✓ WIRED | L71: handleSubmit fetch |
| `admin/students/page.tsx` | `/api/admin/users/:id/status` | `fetch PUT` | ✓ WIRED | L113: toggleStatus function |
| `reset-password-dialog.tsx` | `/api/admin/users/:id/reset-password` | `fetch POST` | ✓ WIRED | L59: handleSubmit fetch |
| `student/layout.tsx` | `student-navbar.tsx` | import + render | ✓ WIRED | L2 import, L7 render |
| `middleware.ts` | `/register` | PUBLIC_PATHS | ✓ WIRED | L5: includes "/register" |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `admin/students/page.tsx` | `users` (useState) | `GET /api/admin/users` → backend `listUsers()` | DB query `db.select(...).from(users)` with pagination | ✓ FLOWING |
| `student-navbar.tsx` | `user` (useState) | `GET /api/auth/me` → backend `getUserById` via session | DB query `db.query.users.findFirst` | ✓ FLOWING |
| `admin-sidebar.tsx` | `user` (useState) | `GET /api/auth/me` → backend `getUserById` via session | DB query `db.query.users.findFirst` | ✓ FLOWING |
| `register/actions.ts` | session cookie | `POST /api/auth/register` → `registerUserWithRole` → `createSession` | DB insert + session creation | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Backend typecheck | `cd packages/backend && bun run typecheck` | Passes | ✓ PASS |
| Frontend typecheck | `cd apps/web && bun run typecheck` | Passes (no output = clean) | ✓ PASS |
| Phase 2.1 auth tests | `cd packages/backend && bun test src/__tests__/auth-register.test.ts` | 12 pass, 0 fail | ✓ PASS |
| Backend admin routes file exists | `wc -l packages/backend/src/routes/admin-users.ts` | 256 lines | ✓ PASS |
| Register page has "创建账号" title | grep in register/page.tsx | L64: `创建账号` | ✓ PASS |
| Login page has register link | grep in login/page.tsx | L92-93: `没有账号？去注册` → `/register` | ✓ PASS |
| Register page has login link | grep in register/page.tsx | L123-124: `已有账号？去登录` → `/login` | ✓ PASS |
| StudentNavbar has "登出" | grep student-navbar.tsx | L80: `登出` | ✓ PASS |
| Admin sidebar has "登出" | grep admin-sidebar.tsx | L136: `登出` | ✓ PASS |
| Middleware has /register in PUBLIC_PATHS | grep middleware.ts | L5: `"/register"` | ✓ PASS |
| Seed uses ROOT_USERNAME | grep admin.seed.ts | L25: `ROOT_USERNAME` | ✓ PASS |
| registerUserWithRole rejects ROOT | grep auth.service.ts | L22-23: role check rejects non-STUDENT/ADMIN | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| AUTH-05 | Plan 01, 02 | 所有用户可登出，登出后清除会话并跳转登录页 | ✓ SATISFIED | Backend: `POST /api/auth/logout` calls `destroySession`. Frontend: StudentNavbar + AdminSidebar handleLogout() clears cookie + redirects to /login |
| AUTH-06 | Plan 01, 03 | 学员和教员可修改自己的密码 | ✓ SATISFIED | Backend: `POST /api/auth/change-password` + `changePassword` service. Frontend: `PasswordChangeForm` used in `student/settings/page.tsx` |
| AUTH-07 | Plan 01, 03 | 管理员可修改自己的密码 | ✓ SATISFIED | Same `changePassword` endpoint + `PasswordChangeForm` used in `admin/settings/page.tsx` |
| UI-AUTH-01 | Plan 02 | 学员/教员可自助注册，注册成功自动登录跳转 | ✓ SATISFIED | `/register` page with student/teacher roles, server action auto-login via cookie |
| UI-AUTH-02 | Plan 02 | 导航有登出按钮，点击清除会话跳转登录页 | ✓ SATISFIED | StudentNavbar dropdown + AdminSidebar bottom area both have logout |
| UI-AUTH-03 | Plan 03 | 设置页面可修改密码（旧+新+确认），成功后保持登录 | ✓ SATISFIED | PasswordChangeForm: 3 fields, toast success, no redirect |
| ADMIN-05 | Plan 01, 03 | 管理员可管理学员账号（查看/禁用/启用/重置密码） | ✓ SATISFIED | `/admin/students` page with table, toggle status, reset password dialog |
| ADMIN-06 | Plan 01, 03 | 管理员可添加教员账号 | ✓ SATISFIED | `AddUserDialog` with role select (教员=10), backend `createUserByAdmin` |
| ADMIN-07 | Plan 01, 03 | 管理员可添加学员账号 | ✓ SATISFIED | `AddUserDialog` with role select (学员=1), backend `createUserByAdmin` |

No orphaned requirements found — all 9 requirement IDs from REQUIREMENTS.md Phase 2.1 are covered by the 3 plans.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | — |

No anti-patterns found. All "placeholder" occurrences are legitimate HTML input placeholders. No TODO/FIXME/stub patterns. No empty implementations.

**Note:** Pre-existing test failures in `password.test.ts` (4 failures due to bun test mock pollution) are from Phase 1, not introduced by Phase 2.1. Phase 2.1's own tests (`auth-register.test.ts`: 12/12 pass) are clean.

### Human Verification Required

### 1. 注册完整流程（学员）

**Test:** 启动前后端服务器 → 访问 /register → 选择学员角色 → 填写用户名+密码+确认密码 → 点击注册
**Expected:** 注册成功自动跳转到 /student/dashboard，顶部 Navbar 显示用户头像和名称
**Why human:** 需要验证跨端口（NextJS→Hono）Set-Cookie 提取和自动跳转行为，以及 client component fetch 携带 cookie 到不同端口后端的实际效果

### 2. 注册完整流程（教员）

**Test:** 访问 /register → 选择教员角色 → 填写信息 → 注册
**Expected:** 注册成功自动跳转到 /admin/dashboard，侧边栏显示用户信息
**Why human:** 需要验证角色判断跳转逻辑（role >= 10 → admin）

### 3. 登录↔注册页面链接

**Test:** 在 /login 点击"没有账号？去注册" → 在 /register 点击"已有账号？去登录"
**Expected:** 正确在两个页面间导航
**Why human:** 简单但需确认视觉渲染正确

### 4. 学员端登出

**Test:** 以学员身份登录 → 点击头像下拉 → 登出
**Expected:** 清除 cookie，跳转到 /login 页面
**Why human:** 需验证 cookie 清除生效，重定向行为正确

### 5. 管理端登出（折叠/展开）

**Test:** 以管理员登录 → 侧边栏展开时点击"登出" → 重新登录 → 折叠侧边栏 → 点击登出图标
**Expected:** 两种状态都能正确登出
**Why human:** 需验证侧边栏两种布局下的登出按钮均可点击

### 6. 学员修改密码

**Test:** 登录学员 → 访问 /student/settings → 输入旧密码+新密码+确认新密码 → 提交
**Expected:** 显示"密码修改成功" toast，保持登录状态
**Why human:** 需验证跨端口 cookie 传递（client component 从 document.cookie 提取 session_id → 携带到 Hono 后端）的实际效果

### 7. 管理员用户管理全流程

**Test:** 登录管理员 → /admin/students → 点击"添加用户" → 选择学员 → 填写信息 → 创建 → 在表格中找到新用户 → 点击"禁用" → 点击"启用" → 点击"重置密码" → 输入新密码
**Expected:** 表格实时刷新，每次操作有 toast 反馈，状态 Badge 正确切换
**Why human:** 多步交互流程，需要验证 Dialog 打开/关闭、表格刷新、toast 时机的完整用户体验

### Gaps Summary

No gaps found. All 8 success criteria are substantively implemented with proper backend-frontend wiring. The codebase shows:
- Complete backend API layer (seed migration, role-based registration, change password, admin CRUD)
- Complete frontend pages (register, login cross-link, student navbar, admin sidebar, settings, user management)
- All key links verified: every frontend component correctly calls its corresponding backend endpoint
- Data flows verified: all API endpoints perform real DB queries, not static returns
- No anti-patterns (stubs, placeholders, TODOs)
- Typecheck passes, Phase 2.1 tests pass (12/12)

The only remaining step is human end-to-end testing to verify the cross-origin cookie flow and interactive UX.

---

_Verified: 2026-04-25T12:51:47Z_
_Verifier: the agent (wsf-verifier)_
