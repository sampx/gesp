---
status: resolved
phase: 02.1-用户基础流程补齐
source: [02.1-01-SUMMARY.md, 02.1-02-SUMMARY.md, 02.1-03-SUMMARY.md, 02.1-04-SUMMARY.md, 02.1-05-SUMMARY.md, 02.1-06-SUMMARY.md, 02.1-07-SUMMARY.md]
started: 2026-04-25T05:12:22Z
updated: 2026-04-25T19:45:36Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running backend/frontend processes. Start from scratch (bun run dev). Backend boots without errors, root admin seeds successfully. Frontend loads at http://localhost:3001, login page renders correctly.
result: pass

### 2. Root Admin Login
expected: Login with root credentials (ROOT_USERNAME/ROOT_PASSWORD from .env). Redirects to /admin/students. Admin sidebar shows root user avatar, username, and ROOT role badge at bottom.
result: issue
reported: "可以登录, 成功后跳转到了 dashboard, 左下侧的显示不太对齐, 这只是一个小缺陷"
severity: cosmetic

### 3. Student Self-Registration
expected: From login page click register link. Register page shows two role cards (Student/Teacher). Select Student, fill username+password+display_name. Submit redirects to /student dashboard. Student navbar shows user avatar and dropdown.
result: issue
reported: "学员注册成功, 但注册页仍显示教员角色卡, 选择教员注册会报错. 用户决定移除教员自助注册, 注册页应隐藏教员选项"
severity: major

### 4. Teacher Self-Registration
expected: On register page, select Teacher role card. Fill username+password+display_name. Submit redirects to corresponding dashboard with navbar showing user info.
result: skipped
reason: Merged into test 3 — teacher self-registration removed per user decision

### 5. Login-Register Cross-links
expected: Login page bottom shows "没有账号？去注册" link. Register page bottom shows "已有账号？去登录" link. Both navigate correctly.
result: pass

### 6. Student Navbar User Dropdown
expected: Click avatar on student navbar. Dropdown shows settings and logout options. Click settings navigates to /student/settings.
result: pass

### 7. Admin Sidebar User Area
expected: Admin pages show sidebar with user area at bottom: avatar circle, display name, role badge (e.g. ROOT/ADMIN), and logout button.
result: pass

### 8. Student Password Change
expected: Go to /student/settings. Password change form shows 3 fields (old password, new password, confirm). Enter valid old password + new password. Success toast appears, no page redirect.
result: pass

### 9. Admin Password Change
expected: Go to /admin/settings. Password change form works identically to student settings. Additional "系统配置" placeholder section visible.
result: pass

### 10. Admin User List
expected: Go to /admin/students. User table shows columns: username, display name, role badge, status badge, actions (toggle/reset). At least root user visible.
result: pass

### 11. Admin Add User
expected: Click add user button. Dialog opens with role select, username, password fields. Select role, fill fields, submit. New user appears in table.
result: pass

### 12. Admin Toggle User Status
expected: Click status toggle on a non-root user. User status changes (active↔disabled). Toast confirms change. Toggle button reflects new state. Attempting to toggle ROOT user shows error.
result: pass

### 13. Admin Reset User Password
expected: Click reset password button on a non-root user. Dialog opens with new password + confirm fields. Submit, toast confirms reset. Attempting to reset ROOT user password shows error.
result: pass

### 14. Logout Flow
expected: Click logout (navbar dropdown or sidebar button). No confirmation dialog. Redirects to /login page. Accessing protected pages redirects to /login.
result: issue
reported: "登出流程正常, 但用户希望添加确认框"
severity: minor

### 15. Student Cannot Access Admin Pages
expected: Login as student. Manually navigate to /admin/students. Middleware redirects to /login?error=forbidden with appropriate error message.
result: pass

### 16. Registration Role Restriction
expected: Register page only shows Student and Teacher role cards. No Admin or ROOT option visible. Backend rejects any attempt to register with ADMIN/ROOT role.
result: skipped
reason: Pending fix from test 3 gap — teacher card removal not yet implemented

### 17. Error Messages in Chinese
expected: Trigger errors (wrong password, duplicate username, short password). All error messages and toasts are in Chinese, not English.
result: issue
reported: "相同用户名可以重复注册, 比如已有用户 sam, 再次用 sam 注册未报错"
severity: major

### 18. Admin Sidebar Consistency
expected: All admin pages show sidebar with user area at bottom consistently.
result: issue
reported: "知识库菜单点击后, 左侧底部无用户信息和登出按钮"
severity: major

### 19. Admin 404 Page Framework
expected: Unimplemented admin pages should retain page framework (sidebar + header), show placeholder in content area.
result: issue
reported: "分析菜单点击后全屏显示 404, 应保留页面框架, 右侧显示功能未实现"
severity: major

## Summary

total: 19
passed: 12
issues: 6
pending: 0
skipped: 1
blocked: 0

## Gaps

- truth: "Admin sidebar user area (avatar, username, role badge) aligned properly at bottom"
  status: resolved
  reason: "User reported: 左下侧的显示不太对齐, 这只是一个小缺陷"
  severity: cosmetic
  test: 2
  root_cause: "Badge 作为独立 block 元素与头像/用户名行视觉脱离，呈现为悬浮 chip 而非内联或与上方用户信息视觉成组"
  artifacts:
    - path: "apps/web/src/components/admin-sidebar.tsx"
      lines: "116-118"
      issue: "Badge 在 space-y-2 中作为独立 block 元素渲染"
  missing:
    - "Badge 应内联到用户名旁边，或与头像+用户名视觉成组"
  debug_session: .planning/debug/sidebar-alignment.md

- truth: "Registration page only shows available roles; selecting a role should always succeed"
  status: resolved
  reason: "User reported: 注册页仍显示教员角色卡但后端拒绝教员注册. 用户决定移除教员自助注册, 注册页应只展示学员角色"
  severity: major
  test: 3
  root_cause: "前后端不同步 — 后端 auth.ts 已限制仅允许 STUDENT 角色自助注册，但前端注册页面从未同步更新以移除教师角色卡片"
  artifacts:
    - path: "apps/web/src/app/register/page.tsx"
      lines: "69-83"
      issue: "仍渲染两个 RoleCard（学员+教员），state 类型包含 teacher"
    - path: "apps/web/src/app/register/actions.ts"
      lines: "17-21"
      issue: "roleMap 仍包含 teacher→ROLE.ADMIN 映射"
  missing:
    - "移除教师 RoleCard，state 类型改为只接受 student"
    - "移除 roleMap 中 teacher 映射，或硬编码 ROLE.STUDENT"
  debug_session: .planning/debug/teacher-role-card-on-register.md

- truth: "Logout should show confirmation dialog before proceeding"
  status: resolved
  reason: "User reported: 希望添加登出确认框"
  severity: minor
  test: 14
  root_cause: "handleLogout 直接调用 logoutAction() 无任何确认守卫，无 dialog、confirm() 或状态检查"
  artifacts:
    - path: "apps/web/src/components/admin-sidebar.tsx"
      lines: "58-61, 123, 136"
      issue: "handleLogout 直接调用 API 后 redirect"
    - path: "apps/web/src/components/student-navbar.tsx"
      lines: "36-39, 67"
      issue: "同样无确认守卫"
  missing:
    - "添加 AlertDialog 或 Dialog 确认步骤"
    - "handleLogout 应在用户明确确认后才执行"
  debug_session: .planning/debug/logout-no-confirmation.md

- truth: "Duplicate username registration should be rejected with error"
  status: resolved
  reason: "User reported: 相同用户名可以重复注册, 比如已有用户 sam, 再次用 sam 注册未报错"
  severity: major
  test: 17
  root_cause: "前后端代码逻辑正确（后端有 findFirst 检查、前端有错误处理），疑为数据库层面 username unique 约束未实际生效（db:push 未执行或 schema 同步失败），导致 findFirst 查询行为异常"
  artifacts:
    - path: "packages/backend/src/db/schema/users.ts"
      issue: "Schema 定义 .unique() 但约束可能未在 DB 层生效"
    - path: "packages/backend/src/services/auth.service.ts"
      lines: "32-38"
      issue: "findFirst 查询可能返回 stale/undefined"
  missing:
    - "验证数据库状态：检查 users 表是否有 username unique index"
    - "若约束缺失：手动创建 unique index 或使用 Drizzle migration"
    - "添加数据库层日志确认 findFirst 行为"
  debug_session: .planning/debug/duplicate-username-registration.md

- truth: "Admin sidebar user area should appear on all admin pages"
  status: resolved
  reason: "User reported: 知识库菜单点击后, 左侧底部无用户信息和登出按钮"
  severity: major
  test: 18
  root_cause: "sidebar 使用 min-h-screen 无 sticky 定位，知识库 DataTable 渲染 20 行无高度约束，内容超长时 sidebar 被拉伸超出视口，mt-auto 将底部区域推至视口外"
  artifacts:
    - path: "apps/web/src/app/admin/layout.tsx"
      lines: "6"
      issue: "外层 flex 容器用 min-h-screen，sidebar 被拉伸匹配内容高度"
    - path: "apps/web/src/components/admin-sidebar.tsx"
      lines: "66, 103"
      issue: "aside 用 min-h-screen 无 sticky，无 overflow-y-auto"
    - path: "apps/web/src/components/knowledge-data-table.tsx"
      issue: "渲染所有行无高度约束，触发 overflow 条件"
  missing:
    - "sidebar 添加 sticky top-0 h-screen overflow-y-auto"
    - "或外层布局改用 h-screen + 内容区 overflow-auto"
  debug_session: .planning/debug/knowledge-page-sidebar.md

- truth: "Unimplemented admin pages should retain sidebar framework and show placeholder"
  status: resolved
  reason: "User reported: 分析菜单点击后全屏显示 404, 应保留页面框架, 右侧显示功能未实现"
  severity: major
  test: 19
  root_cause: "/admin/analytics/page.tsx 文件不存在，Next.js 默认 404 页面全屏渲染而非保留 admin layout 框架"
  artifacts: []
  missing:
    - "创建 /admin/analytics/page.tsx 占位页面"
    - "页面内容显示'功能未实现'提示，保留 sidebar 框架"
  debug_session: .planning/debug/analytics-404.md
