---
phase: 02.1-用户基础流程补齐
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/admin/layout.tsx
  - apps/web/src/components/admin-sidebar.tsx
  - apps/web/src/components/student-navbar.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Admin sidebar user area (avatar, username, role badge) aligned as a visual group"
    - "Logout shows AlertDialog confirmation before proceeding"
    - "Admin sidebar stays fixed and visible on all pages regardless of content height"
  artifacts:
    - path: "apps/web/src/app/admin/layout.tsx"
      provides: "h-screen layout with overflow-auto content area"
      contains: "h-screen"
    - path: "apps/web/src/components/admin-sidebar.tsx"
      provides: "Sticky sidebar with fixed bottom user area + logout confirmation"
      contains: "sticky"
    - path: "apps/web/src/components/student-navbar.tsx"
      provides: "Student navbar with logout confirmation AlertDialog"
      contains: "AlertDialog"
  key_links:
    - from: "apps/web/src/app/admin/layout.tsx"
      to: "apps/web/src/components/admin-sidebar.tsx"
      via: "AdminSidebar component import"
      pattern: "AdminSidebar"
---

<objective>
Fix admin layout and sidebar: make sidebar sticky with proper user area alignment, add logout confirmation dialogs to both admin sidebar and student navbar.

Purpose: Close UAT gaps #1 (cosmetic badge alignment), #3 (minor logout confirmation), #5 (major sidebar disappearing on long pages).
Output: Properly aligned, sticky sidebar; confirmation-guarded logout.
</objective>

<execution_context>
@/Users/sam/coding/wopal/wopal-workspace/.agents/wsf/workflows/execute-plan.md
@/Users/sam/coding/wopal/wopal-workspace/.agents/wsf/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-用户基础流程补齐/02.1-02-SUMMARY.md

<interfaces>
<!-- Current admin-sidebar.tsx key structure -->
From apps/web/src/components/admin-sidebar.tsx:
```typescript
// Line 66: aside uses "min-h-screen" — no sticky, no overflow
// Line 103: mt-auto div with space-y-2 layout
// Line 116-118: Badge as independent block element in space-y-2
// Line 58-61: handleLogout directly calls logoutAction() + redirect
```

From apps/web/src/components/student-navbar.tsx:
```typescript
// Line 36-39: handleLogout directly calls logoutAction() + redirect
```

From apps/web/src/app/admin/layout.tsx:
```typescript
// Line 6: outer div uses "min-h-screen flex" — stretches with content
// Line 8: content div uses "flex-1 min-w-0" — no overflow constraint
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix admin layout — h-screen + sticky sidebar + badge alignment</name>
  <files>apps/web/src/app/admin/layout.tsx, apps/web/src/components/admin-sidebar.tsx</files>
  <action>
Fix the admin layout so sidebar stays fixed regardless of content height, and align badge with user info.

**1. admin/layout.tsx** — Change outer container from min-h-screen to h-screen, add overflow-auto to content area:
- Line 6: Change `"min-h-screen flex"` to `"h-screen flex overflow-hidden"`
- Line 8: Change `"flex-1 min-w-0"` to `"flex-1 min-w-0 overflow-y-auto"`

**2. admin-sidebar.tsx** — Fix sticky and badge alignment:
- Line 66: Change aside className from `"border-r bg-card flex flex-col min-h-screen transition-all duration-300"` to `"border-r bg-card flex flex-col h-screen sticky top-0 transition-all duration-300 overflow-y-auto"`
- Lines 105-128 (expanded user area): Fix badge alignment by making it inline with the username:
  - Replace the `space-y-2` div (line 105) with a tighter layout
  - Move the Badge from a separate block to inline next to the username on line 112-114
  - The structure should be: avatar + username row (with badge inline after username), then logout button
  - Example: `<div className="flex items-center gap-2">` → avatar → `<div className="flex-1 min-w-0">` → `<div className="flex items-center gap-1.5">` → username span + Badge inline → `</div></div>`
  - Logout button remains below as a full-width button
  </action>
  <verify>
    <automated>cd /Users/sam/coding/wopal/wopal-workspace/projects/gesp && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Sidebar is sticky with h-screen, badge is inline with username, layout uses h-screen with overflow-y-auto on content area</done>
</task>

<task type="auto">
  <name>Task 2: Add AlertDialog logout confirmation to both nav components</name>
  <files>apps/web/src/components/admin-sidebar.tsx, apps/web/src/components/student-navbar.tsx</files>
  <action>
Add logout confirmation using AlertDialog (shadcn/ui). Since AlertDialog component doesn't exist yet, first install it, then add confirmation to both components.

**0. Install AlertDialog:**
Run `npx shadcn@latest add alert-dialog -y` in the web app directory. If this fails or is not available, create the AlertDialog component manually following shadcn/ui pattern using @radix-ui/react-alert-dialog.

**1. admin-sidebar.tsx** — Wrap handleLogout with AlertDialog:
- Import AlertDialog components: `AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger`
- Replace the logout Button (lines 119-127 expanded, lines 136-138 collapsed) with an AlertDialogTrigger wrapping the same button
- AlertDialogContent: title="确认登出", description="确定要退出登录吗？", cancel button, confirm button (AlertDialogAction onClick={handleLogout})
- Apply to BOTH expanded and collapsed sidebar states

**2. student-navbar.tsx** — Same pattern:
- Import AlertDialog components
- Replace the logout DropdownMenuItem (line 67) with a DropdownMenuItem that opens an AlertDialog
- OR: Replace the DropdownMenuItem onClick with a state-based AlertDialog trigger
- AlertDialogContent: title="确认登出", description="确定要退出登录吗？", cancel + confirm buttons
- The actual logout only fires on AlertDialogAction click
  </action>
  <verify>
    <automated>cd /Users/sam/coding/wopal/wopal-workspace/projects/gesp && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Both admin sidebar and student navbar show AlertDialog confirmation before logout executes</done>
</task>

</tasks>

<verification>
1. Open any admin page with long content (e.g., knowledge points) — sidebar stays fixed, user area visible at bottom
2. Click logout in admin sidebar — confirmation dialog appears, cancel keeps user logged in, confirm logs out
3. Click logout in student navbar — confirmation dialog appears, same behavior
4. Badge appears inline with username, not as detached chip
</verification>

<success_criteria>
- Admin sidebar sticky with h-screen, content area scrollable independently
- Badge visually grouped with avatar + username (inline, not detached block)
- Both logout flows show AlertDialog before executing
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-用户基础流程补齐/02.1-04-SUMMARY.md`
</output>
