---
phase: 02.1-用户基础流程补齐
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/admin/analytics/page.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Clicking analytics menu shows admin layout with sidebar and placeholder content"
  artifacts:
    - path: "apps/web/src/app/admin/analytics/page.tsx"
      provides: "Analytics placeholder page within admin layout"
      contains: "功能"
  key_links:
    - from: "apps/web/src/components/admin-sidebar.tsx"
      to: "apps/web/src/app/admin/analytics/page.tsx"
      via: "sidebar nav link /admin/analytics"
      pattern: "admin/analytics"
---

<objective>
Create /admin/analytics/page.tsx placeholder page so clicking the analytics menu item shows the admin layout framework with a "feature not implemented" message instead of a full-screen 404.

Purpose: Close UAT gap #6 (major) — analytics menu click shows full-screen 404 instead of keeping sidebar framework.
Output: Placeholder page within admin layout.
</objective>

<execution_context>
@/Users/sam/coding/wopal/wopal-workspace/.agents/wsf/workflows/execute-plan.md
@/Users/sam/coding/wopal/wopal-workspace/.agents/wsf/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-用户基础流程补齐/02.1-02-SUMMARY.md

<interfaces>
From apps/web/src/components/admin-sidebar.tsx:
```typescript
// Line 33: { href: "/admin/analytics", label: "数据分析", icon: BarChart3 }
```

Admin layout pattern (from existing pages like admin/students/page.tsx):
```typescript
// All admin pages are client components using "use client"
// Use Card component for content areas
// Access backend via document.cookie extraction pattern
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analytics placeholder page</name>
  <files>apps/web/src/app/admin/analytics/page.tsx</files>
  <action>
Create a new file `apps/web/src/app/admin/analytics/page.tsx` as a placeholder page within the admin layout.

The page should:
- Use a simple functional component (no "use client" needed unless interactive)
- Import Card, CardContent, CardHeader, CardTitle from @/components/ui/card
- Import BarChart3 icon from lucide-react (matching sidebar icon)
- Display a centered placeholder message in Chinese: "数据分析功能即将上线" with a brief description "该功能将在后续版本中实现，敬请期待"
- Wrap in a Card component with an icon and title, matching the visual style of other admin pages
- Keep it simple — no data fetching, no client-side state, just a static placeholder

Example structure:
```tsx
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { BarChart3 } from "lucide-react";

export default function AnalyticsPage() {
  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-bold">数据分析</h1>
      <Card>
        <CardHeader>
          <CardTitle className="flex items-center gap-2">
            <BarChart3 className="h-5 w-5" />
            数据分析
          </CardTitle>
        </CardHeader>
        <CardContent className="text-center py-12">
          <p className="text-muted-foreground">数据分析功能即将上线</p>
          <p className="text-sm text-muted-foreground mt-2">该功能将在后续版本中实现，敬请期待</p>
        </CardContent>
      </Card>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/sam/coding/wopal/wopal-workspace/projects/gesp && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>File apps/web/src/app/admin/analytics/page.tsx exists with placeholder content, renders within admin layout with sidebar visible</done>
</task>

</tasks>

<verification>
1. Click "数据分析" in admin sidebar → page loads with sidebar frame visible
2. Content area shows "数据分析功能即将上线" placeholder
3. No full-screen 404
4. TypeScript compiles without errors
</verification>

<success_criteria>
- /admin/analytics renders within admin layout (sidebar visible)
- Placeholder message in Chinese displayed
- No 404 error
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-用户基础流程补齐/02.1-07-SUMMARY.md`
</output>
