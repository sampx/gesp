---
phase: 02.1-用户基础流程补齐
plan: 06
subsystem: auth
tags: [sqlite, drizzle-orm, unique-constraint, duplicate-check, tdd]

# Dependency graph
requires:
  - phase: 02.1-01
    provides: "auth.service.ts with registerUserWithRole and createUserByAdmin"
provides:
  - "Defensive try/catch around db.insert for UNIQUE constraint violations"
  - "Test coverage for duplicate username rejection via both registration endpoints"
affects: [auth, registration]

# Tech tracking
tech-stack:
  added: []
  patterns: ["defensive-try-catch-for-db-constraint", "race-condition-guard"]

key-files:
  created: []
  modified:
    - "packages/backend/src/services/auth.service.ts"
    - "packages/backend/src/__tests__/auth-register.test.ts"

key-decisions:
  - "Wrap db.insert in try/catch to catch SQLITE_CONSTRAINT_UNIQUE as defense against race conditions"
  - "Service-layer findFirst check remains primary guard; DB constraint is secondary safety net"

patterns-established:
  - "Defensive DB insert: try/catch wrapping with constraint error detection for all user creation paths"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-04-25
---

# Phase 02.1 Plan 06: Duplicate Username Rejection Summary

**Defensive try/catch around db.insert catching SQLITE_CONSTRAINT_UNIQUE race conditions in both registerUserWithRole and createUserByAdmin**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-25T06:32:58Z
- **Completed:** 2026-04-25T06:36:59Z
- **Tasks:** 1 (TDD)
- **Files modified:** 2

## Accomplishments
- Identified that service-layer `findFirst` check works correctly but lacks defense against race conditions
- Added try/catch around `db.insert` in both `registerUserWithRole` and `createUserByAdmin` to catch UNIQUE constraint violations
- Added 3 test cases proving duplicate rejection via both endpoints + race condition constraint error handling

## Task Commits

1. **Task 1: Add duplicate username test + enforce DB constraint** - `819ec78` (fix)

## Files Created/Modified
- `packages/backend/src/services/auth.service.ts` - Added defensive try/catch around db.insert in registerUserWithRole and createUserByAdmin
- `packages/backend/src/__tests__/auth-register.test.ts` - Added 3 test cases: duplicate via registerUserWithRole, duplicate via createUserByAdmin, race condition constraint violation

## Decisions Made
- Used try/catch wrapping as secondary defense rather than relying solely on `findFirst` check — protects against race conditions where two concurrent requests both pass the `findFirst` check
- Kept the existing `findFirst` check as primary guard for user-friendly error messages and faster response

## Deviations from Plan

None - plan executed exactly as written. TDD flow confirmed: test proved the gap (race condition constraint error not caught), fix added defensive try/catch, all tests pass.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Duplicate username registration fully handled at both service and DB constraint levels
- Ready for remaining Phase 02.1 plans

---
*Phase: 02.1-用户基础流程补齐*
*Completed: 2026-04-25*

## Self-Check: PASSED

- FOUND: packages/backend/src/services/auth.service.ts
- FOUND: packages/backend/src/__tests__/auth-register.test.ts
- FOUND: .planning/phases/02.1-用户基础流程补齐/02.1-06-SUMMARY.md
- FOUND: commit 819ec78
